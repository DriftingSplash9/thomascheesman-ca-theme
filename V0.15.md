# V0.15 — Secret drawer engine, email system, full puzzle design

**Date wrapped:** 2026-05-23
**Theme version at wrap:** 1.0.341
**WordPress at wrap:** 7.0 "Armstrong"
**Astra at wrap:** 4.13.x

Successor to V0.14. The next agent writes **V0.16**.

---

## ⚠️ Read this first — push state

Every code change this session is committed and **pushed to `origin/main`** and auto-deployed by Hostinger. Last code commit: `c2a2bb5` (theme 1.0.341, author mode).

The V0.15 commit (this doc) won't be on `origin/main` until pushed. Check + push:

```
git log origin/main..HEAD --oneline
git push origin HEAD:main
```

Pure docs — no theme-version bump.

The **agent role was expanded twice** this session — caps now include `edit_posts`, `edit_others_posts`, `delete_posts`, `delete_others_posts`, `delete_published_posts`. Page-deletion caps remain denied. `TC_AGENT_ROLE_VERSION` is now **3**; re-registration is automatic on the next `init` hit. If you need it to apply: LiteSpeed Purge All → load any page → done.

---

## ▶ NEXT TASK — Phase 4: wire the real puzzle into `drawer-puzzle.json`

The full **secret-drawer puzzle is fully designed**. Every chain, every prize, every cut item, every easter egg. **The next session is data entry**: populate `inc/data/drawer-puzzle.json` with the real items, surfaces, zones, and interactions from the two off-repo design docs.

**The design specs live on Thomas's Desktop** (not in the repo — they're planning artifacts):

- **`C:\Users\thoma\Desktop\Secret-Drawer-Items (NEW2).docx`** — the **Inventory**. 87 items in 18 groups. Every item has a WP ID + role + chain assignment + status (★ CORE, ✓ confirmed, ✗ cut, ⌫ WP-deleted). Includes a Plans section + a Hangman words appendix (12 monthly themes × 20 words each).
- **`C:\Users\thoma\Desktop\Secret-Drawer-Playbook.docx`** — the **Playbook**. A flowchart of every chain (rendered via kroki.io Mermaid → PNG, embedded on a landscape page) + a player-POV walkthrough that reads top-to-bottom as the puzzle unfolds. Use this to *see* the design; use the Inventory to look up specific items.

**The TL;DR puzzle shape** (so the next agent doesn't have to read the docs cold):

The premise: *"Clean up my junk drawer and win the top prize."* When the drawer opens, the **You Got This notebook** sits on top with the to-do list: **Find Ring · Tidy drawer · Dust**. Dust is last on purpose — the Golden Egg path is to do everything else and **deliberately leave the dust**.

Three prizes:
- **TOP PRIZE** = Ledger Wallet (#3682), in the secret drawer behind the padlock. Needs the **Padlock Keys + Kid Charms** (#3704/#3701) to open AND the **67676767 passcode** from the wallet.
- **BIGGER REWARD** = Faberge Egg D'Elegance ($264M). Foreshadowed by the Hulk-smash chain (Torch Lighter on a Ring reveals *"one PEZ to rule them all"*; PEZ dispenses the Auction Receipt). Actually delivered as the **Faberge video (#3727)** that plays when the player **sells the banana** for $6.4M to commission it.
- **GOLDEN EGG** = Duct-Taped Banana (#3726, the hero shot). Path: leave the dust + combine raw Banana (#3616) with Gorilla Tape (#3723). Player choice: **eat** (delicious) or **sell** ($6.4M → Faberge video).

The chains (group numbering matches the Inventory):
1. Start: Screwdriver → Loose Screw → Dust → Green Duster → Duck Pin
2. Hulk smash: Hulk-no-hammer + Hulk's Hammer → Armed Hulk → Silver Nugget → Rings → Torch Lighter clue → PEZ → Auction Receipt
3. Giant-duck gag: Rubber Duck + Mini Air Pump → small → midway → giant. **Giant duck's head crushes the WHOLE browser** via the Fullscreen API.
4. Rolled Scroll + BIC Lighter → Bitcoin whitepaper. BIC also lights the Jack-o-Lantern.
5. Hot Wheels race (still needs the race-shoot video with Thomas's son — Phase 5).
6. Glasses chain: Ensure (alertness) → Glasses Case → Eyeglasses → Clip-On Sunglasses → Sunglasses on → shaded view reveals hidden text on the bottom of the drawer: *"See you again Dec. 1st 2026!!!"* — the Elf prelude (same line as the Polaroid).
7. Batteries + electronics: AAA needs TWO sets (split the image) — one for the Toy Train (TV Remote is cut, iPhone is direct). Car Keys carry a small screwdriver for battery covers.
8. Padlock chain → unlocks the secret drawer.
9. Hangman game (uses Sticky Notes + Mechanical Pencil; word list = 12-month rotation in the Inventory appendix).
10. Antique Book opens → Open Bible → click → **Proverbs 25:2** (*"It is the glory of God to conceal a thing: but the honour of kings is to search out a matter"*).
11. Olestyr packet (cut from puzzle; scissors open it, useless after).
12. Sentimental keepsakes (Spinning Top + Toy Jet — small things, outsized meaning).
13. Marbles → one replaces the footer's floating marble; pinball ball colour matches.
14. The Prizes (above).
15. Wallet chain: Flip Middle Finger Figurine → 67 Sticker → Bifold Wallet collects ($20 Bills, GP Gift Card, 67 Sticker, Coins). Wallet yields the 67676767 passcode (the 67 sticker is the hint). **Lifting the GP Gift Card reveals the Polaroid (#3722) underneath** — the Jake letter.
16. Standalone easter eggs + "this is me" lore (astronaut sticker → black-hole effect; Pac-Man → coins to wallet; iPhone → muted funny family videos; etc.)
17. Cut from puzzle — 12 items (junk-drawer dressing).

**The to-do for the next agent**:
1. Open `inc/data/drawer-puzzle.json`. The Phase-2 demo content is in there now (screw → dust → pin + a joke combine).
2. **Replace the demo content** with the real groups from the Inventory + Playbook. Schema is established — `surfaces`, `objects`, `zones`, `interactions` with `on: click | drop | combine`.
3. **Switch surface references** from the old AI-objects compartment art (#3662/#3663) to the **real empty compartments** (#3724 clean, #3725 dusty). Update `functions.php`'s `tcSecretDrawer.assets` block accordingly.
4. Use **author mode (`?author` URL param)** to lay out the new objects visually. Thomas drops them where he wants, hits "Copy layout", pastes the JSON back. Bake the coordinates in.
5. New action verbs likely needed (the engine schema can absorb them):
   - `video:<id>` — play a WP video (the Faberge clip #3727)
   - `fullscreen:<id>` — request `element.requestFullscreen()` (the giant duck head)
   - `branch:<flag>` — branch logic for the dust-cleared-vs-not Golden Egg/Top Prize fork

**The puzzle architecture** is in `assets/js/drawer-engine.js`. Read its docblock first. Author mode is at the bottom of the same file.

---

## What got built this session

**Phase 0 — media library cleanup**
- Audited 1110 media items, identified 289 orphan candidates
- Built `audit_media.py` + `build_contactsheet.py` (in `C:\Users\thoma\Desktop\wp-media-audit\`)
- Thomas reviewed and approved 32 deletions; **32 orphans deleted** (~32 MB reclaimed)

**Phase 1 — loose-handle secret drawer** (commit `3be978c`)
- `footer.php`: the brass pull is now an interactive `<button data-tc-loose-handle>` with a hover bubble and choice popover
- `assets/js/secret-drawer.js`: handle state machine, tighten animation handoff, overlay open/close, focus trap, Escape, body scroll lock, localStorage tightened-state
- `assets/css/secret-drawer.css`: the bubble, choice popover, tighten spin, overlay/scrim/stage, reduced-motion + responsive
- Added `.tc-secret-drawer` overlay markup with the junk-drawer artwork + an empty objects layer (mount point for Phase 2)

**Phase 2 — the interaction engine** (commit `3bd8750`, plus drag-drop fix `c69a8b7`)
- `inc/data/drawer-puzzle.json` — schema + a working demo chain
- `assets/js/drawer-engine.js` — object/zone registry, pointer-based drag (combine + drop), click actions, hover hints, action verbs (`reveal`, `hide`, `remove`, `surface`, `flag`, `clue`, `effect`, `notify`), clue cards, flash effect, localStorage progress keyed by puzzle version
- `assets/css/drawer-engine.css` — objects, placeholder chips, drop zones, hints, drag feedback, clue card
- `secret-drawer.js` updated to lazy-load + boot the engine on first open
- `functions.php` enqueues engine CSS; resolves puzzle media IDs to URLs; localizes everything
- Drag-drop uses **rectangle-overlap hit-testing** (forgiving) + drop targets glow gold while dragging

**13.5 sticker tweaks** (commits `6943ccb`, `54e9cf1`) — `drawer-puzzle.json` data tweaks

**Email system** (commits `c445bf2`, `41e71ea`)
- `inc/email-smtp.php` — routes every `wp_mail()` through SMTP when `TC_SMTP_*` constants are present in wp-config.php. **Thomas added Gmail App Password credentials**; emails now deliver via Gmail SMTP, **straight to inbox (verified)**.
- `inc/drawer-events.php` — public REST endpoint `POST /wp-json/tc-drawer/v1/event`. Whitelist (`test`, `golden-egg`, `top-prize`), 60-second cooldown, daily cap (12), soft same-origin check, timestamps in `America/Edmonton`. Sends to `TC_ALERT_EMAIL` if defined, else `admin_email`.
- Engine gains `notify` action verb → fire-and-forget POST to the endpoint
- **Wired and tested end-to-end**: clicking the demo duck-pin fired a `test` event and the email arrived in Thomas's Gmail.

**Author mode** (commit `c2a2bb5`)
- Add `?author` to any URL → the engine renders **every** object + zone (hidden ones dimmed), makes them all free-drag, disables interactions
- Drag to reposition; **scroll-wheel to resize** (Shift+wheel = a zone's height)
- A toolbar offers **"Copy layout"** which yields the current `objects` + `zones` JSON to the clipboard
- Surface preview buttons let Thomas arrange against any drawer state
- Layout persists in `localStorage` so a reload doesn't lose work
- `TCDrawerEngine.dump()` returns the same JSON from the console

**Agent role expansions**
- Commit `684436b`: added `edit_posts` + `edit_others_posts` (for media title editing)
- Commit `1f3cd75`: added `delete_posts` + `delete_others_posts` + `delete_published_posts` (for media library cleanup)
- Page-deletion caps still denied. `TC_AGENT_ROLE_VERSION = 3`.

**Massive WP media management** (no theme commits; all REST via the agent role)
- Uploaded 47 original cutouts + 26 + 10 new (named all)
- Uploaded 4 Grok drawer backgrounds + 13.5 sticker + reference image
- Uploaded the Polaroid (Jake letter) using Thomas's real blank polaroid template
- Uploaded Gorilla Tape, real empty compartments (#3724/#3725), duct-taped banana hero (#3726), Faberge Egg video clip (#3727)
- Renames: 3612 Rock → Banana Muffin, 3670 Decorative Hook → Hulk's Hammer, 3697 Hair Clip → Nail Clippers
- Deletions: 32 audit orphans, dup Rings #3639, Stylus Pen #3696, old plain banana #3669, my paint-over compartments #3717/#3718

**Off-repo design artifacts created**
- `C:\Users\thoma\Desktop\Secret-Drawer-Items (NEW2).docx` — Inventory (87 items × 18 groups)
- `C:\Users\thoma\Desktop\Secret-Drawer-Playbook.docx` — Playbook (flowchart + walkthrough)
- `C:\Users\thoma\Desktop\drawer-doc\` — the Node.js build scripts for both docs (`make_doc.js`, `make_playbook.js`)
- `C:\Users\thoma\Desktop\drawer-doc\polaroid\` — Polaroid composite assets (PIL scripts + final PNG)

---

## Where things stand (live)

- Site is **live and stable** at https://thomascheesman.ca
- WordPress 7.0 "Armstrong", Astra 4.13.x, theme 1.0.341
- All V0.14 pages still live; nothing new this session
- **The secret drawer is interactive end-to-end**: tighten the loose handle → drawer overlay opens → engine renders the (demo) puzzle → drop the screwdriver on the screw → flash + surface swap + reveal + duck pin → click duck pin → clue card AND a test email arrives in Thomas's Gmail.
- **Author mode** (`?author`) works for laying out new arrangements.
- **SMTP via Gmail** is live and delivering reliably.

---

## Open questions Thomas still owes himself

The Inventory's TO DO list is current. Still open:
- **24 days of Jake mischief** — design (per-day files + story beats). Can wait for November.
- **Auction Receipt video prompt** — Thomas has the still image; needs a prompt for showing it off in a video. He said "more on this later."
- **Handwritten Note (#3674)** — is it the close-up of the note already pictured in the secret-drawer artwork? Affects whether the click-to-read is half-done already.

That's it. Everything else has a decided role.

---

## Sharp edges (carried + new)

1. **LiteSpeed cache** — still the #1 source of "did it deploy?" confusion. Always remind Thomas to Purge All after a push.
2. **Hostinger admin plugin** still suppresses parts of wp-admin UI (App Passwords list etc.). The patterns in `inc/agent-role.php` handle it.
3. **Word locks the docx open** — when regenerating either of the two off-repo docs while Thomas has them open in Word, the write fails with `EBUSY`. Both build scripts fall back to a `(NEW)` / `(NEW2)` suffixed filename. Tell Thomas to close Word and you'll target the canonical filename, or just live with the suffix dance.
4. **kroki.io renders Mermaid** for the Playbook flowchart. If kroki is down, the Playbook build fails. Fallback would be mermaid.ink, or local mermaid-cli (heavy install).
5. **The Phase-2 demo data in `drawer-puzzle.json` will be replaced** by the next agent during Phase 4. Don't preserve it.
6. **The engine config in `functions.php`** still references the OLD Grok compartment images (#3662 with AI key/scroll/potion, #3663 with same). The **real empty compartments are #3724 (clean) and #3725 (dusty)** — swap these IDs in the `wp_localize_script` 'tcSecretDrawer.assets' block during Phase 4. (Same for the duct-taped banana: old `#3669` is deleted; the hero shot is **#3726**.)
7. **The Phase-2 engine's `notify` action** is wired to `inc/drawer-events.php`. The whitelist is `['test', 'golden-egg', 'top-prize']` — add more there if Phase 4 needs them. Rate-limit is 12 emails/day; raise `TC_DRAWER_EVENT_DAILY_CAP` if needed.

---

## Key files / where things live (V0.15 additions)

| Thing | Path |
|---|---|
| Loose-handle interaction | `assets/js/secret-drawer.js` + `assets/css/secret-drawer.css` |
| Phase-2 interaction engine | `assets/js/drawer-engine.js` + `assets/css/drawer-engine.css` |
| Puzzle data (Phase 2 demo) | `inc/data/drawer-puzzle.json` |
| SMTP routing for wp_mail | `inc/email-smtp.php` (creds in wp-config.php — outside repo) |
| Drawer event REST endpoint | `inc/drawer-events.php` |
| Inventory (planning doc) | `C:\Users\thoma\Desktop\Secret-Drawer-Items (NEW2).docx` |
| Playbook (planning doc) | `C:\Users\thoma\Desktop\Secret-Drawer-Playbook.docx` |
| Doc build scripts | `C:\Users\thoma\Desktop\drawer-doc\` (Node + Python) |

---

## Memory pointers (rules to know — see CLAUDE.md for the full set)

- ⭐ **Never auto-commit-and-push.** Propose, wait for "go." Task-scoped only.
- ⭐ **Bump `style.css` `Version:`** on every theme-code commit. Patch increment.
- ⭐ **Never `object-fit: cover`** — always `contain`.
- ⭐ **Desk menu AND drawer footer are BHAG-tier.** Bold ideas. Offer ChatGPT brainstorm prompts before coding starts on either.
- ⭐ **Thomas is programmer-adjacent, not a beginner.** Medium register. Don't over-explain CLI / git / wp-admin basics.
- 🔒 **Thomas + Mel are parenting cohabitants and friends.** Status private. Don't write copy framing them as a couple. Don't surface unprompted.
- **Word salad is fine.** Thomas knows it; just interpret structurally and ask if a key term is ambiguous. He'll redirect.
- **Security warnings still stand:** real BTC seed phrase never photographed (the Ledger's displayed phrase is decoy lyrics); real BTC balance not displayed publicly — the $264M figure is the fictional Faberge auction.
- **Email alerts go to Thomas's Gmail** (`thomasmcheesman@gmail.com`). Rate-limited at the endpoint. Phase-4 ending interactions should fire `notify: "golden-egg"` and `notify: "top-prize"` from the relevant `do` blocks.

---

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`). **Start by reading `CLAUDE.md` and `V0.15.md`** at the repo root — those carry the project rules and full session-15 state.
>
> The secret-drawer engine is built and live. The next task is **Phase 4: wire the real puzzle into `inc/data/drawer-puzzle.json`**. The design specs live on Thomas's Desktop as `Secret-Drawer-Items (NEW2).docx` (87-item inventory grouped into chains) and `Secret-Drawer-Playbook.docx` (flowchart + walkthrough). Read the Playbook first to see the shape; use the Inventory to look up specific items by WP ID.
>
> You can use **author mode** (`?author` URL param) to lay out objects visually before baking coordinates into the JSON. The engine supports `click`, `drop`, and `combine` interactions with actions for reveal/hide/remove/surface/flag/clue/effect/notify. Add new action verbs (probably `video`, `fullscreen`, and a branch primitive for the dust-fork) as needed — they're a switch case in `runActions()`.
>
> When you swap in the real puzzle, **also update `functions.php`'s `tcSecretDrawer.assets`** to point at the real empty compartments (`#3724` clean, `#3725` dusty) instead of the original Grok images with AI objects (#3662/#3663).
>
> Thomas wants to keep iterating — bold ideas welcome, the drawer is BHAG-tier per CLAUDE.md rule 4. Offer a brainstorm prompt before any big design decision; bring options not absolutes.
