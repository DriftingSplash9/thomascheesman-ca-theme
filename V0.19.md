# V0.19 — All eight family lines now have an illustrated long-read (the Cheesman line written + photographed); navigation rebuilt to eight peers; atmosphere, migration, and balance passes done. Next: the Thomas & Melanie pages, then the converging-families map.

**Date wrapped:** 2026-06-02
**Theme version at wrap:** 1.0.434 (pushed)
**WordPress at wrap:** 7.0 "Armstrong" · **Astra:** 4.13.x

Successor to [V0.18.md](V0.18.md). The next agent writes **V0.20**. Read V0.18 + V0.17 for the long-read *system* internals (pipeline, the 10 styling features, sharp edges); this doc is current state + what's next.

---

## ⚠️ Read this first — state

- **All work this session is committed AND pushed.** HEAD = `d7f1006` (**1.0.434**), `origin/main` level (`git log origin/main..HEAD --oneline` is empty).
- **Working tree carries only the five untracked `sk_*.html` McIver scrapes** at repo root. Leave them. **Always `git add` explicit paths, never `git add .`**.
- **⛔ Outstanding manual WP step (blocks the Cheesman long-read):** create a WP page **title "Story", slug `story`, Parent = The Cheesmans**. Until it exists, `/family/heritage/cheesmans/story` 404s (same pattern as the McIver page last session). Confirm it (and the McIver `mcivers` page) exist before "debugging" a broken route. Then **LiteSpeed → Purge All**.
- Authoring/research lives **off-repo** in `C:\Users\thoma\Desktop\Heritage research\`. The converter is `_md2heritage.js`; per-line manuscripts are `{Line}-Story-EDITED.md`; figure maps are `{line}-images.json`. Bump `style.css` Version with the **Edit tool, never `sed`**. **This doc is pure docs — no version bump.**

---

## What this session built (commit by commit)

| Commit | Ver | What |
|---|---|---|
| `9dc7c32` | 1.0.428 | **Task A** — in-text "Notes" → link to the collapsible Notes appendix (`#lr-notes`; `_md2heritage.js` enhancement + `initHeritageNotes()` in main.js to open the `<details>`), and a **full breadcrumb trail** (Family › Heritage › Line › …) via new `inc/heritage-breadcrumb.php`, replacing the single "← back" links on the hub, spokes, and long-reads. |
| `e6ea93d` | 1.0.429 | **Task B** — heritage long-read **atmosphere depth**: base gradient now derives from each line's own `--blob` palette (every line reads in its own colour, not a shared dark slate); stronger radial pools; brighter drifting blobs; lit frosted-glass plate. System-wide. |
| `4f10ec0` | 1.0.430 | **Task C** — restored over-cut **migration/geography** to the Lakeman (Ch 7 Indies route + why + cross-family thread) and Haiste (Atlantic crossing, Aire-valley villages, Dust-Bowl displacement, the convergence pull-quote) manuscripts; amended the editorial philosophy to preserve movement detail. |
| `9f833bb` | 1.0.431 | **Surfaced all eight lines as full peers** across every nav surface (family tree, heritage hub, desk plain/mobile menu), numbered **01–08** (orphans interleaved after their parent). Created `scotland.svg` + `de.svg`; **split the composite Docherty flag → pure Irish `ie.svg`** (McIver now flies its own Saltire). Fixed McIver long-read's missing `--line-color`/blob palette. Plain ("simple") menu kept to **90vh × 100vw**. |
| `052de37` | 1.0.432 | **Balance pass** — trimmed Haiste Ch 2–4 (Newton/Plague survey-history over-reach) and the Lakeman Ch 4 Napoleonic recap; **enriched the under-indexed Verboom** Ch 2 (Sliedrecht dredging/dike world). Logged the family/world balance rule (McIver = the template). |
| `7e56e26` | 1.0.433 | **The Cheesman long-read — the eighth and final line.** First-person, written from the inside; registered in `page-story.php` as line 01 of 08; blob palette + spoke CTA. |
| `d7f1006` | 1.0.434 | **Photos** — 13 dated Cheesman photos + a 1999 family hero through the chapters; **three photos of Martin** added to the Lakeman line's Ch 10 (Martin = the *biological-father* line). Originals optimized 131 MB → ~5 MB. |

### 🎉 Headline: every one of the eight lines now has a long-read.
Cheesmans · Dochertys · **McIvers** · Lakemans · **Verbooms** · Rycrofts · **Steinkes** · Haistes. The Cheesman line (the *chosen* line, Thomas's own story) was the last, and it's now written **and** illustrated.

---

## Key changes to systems & conventions (so the next agent isn't surprised)

1. **Heritage is now EIGHT peers, not "five lines, one household."** Hub copy, family-tree chips, both menus, the About page, and all eyebrows say **eight** and number **01–08** (interleaved: 01 Cheesmans, 02 Dochertys, 03 McIvers, 04 Lakemans, 05 Verbooms, 06 Rycrofts, 07 Steinkes, 08 Haistes). The old "orphan" framing is gone from the nav — McIver/Verboom/Steinke are full chips/cards that link straight to their long-read. Their URLs are still nested (`/family/heritage/dochertys/mcivers`, `/lakemans/verbooms`, `/rycrofts/steinkes`) and they're still standalone `page-{slug}.php` files (not in the `page-story.php` registry).
2. **"father" = Martin (the Lakeman, biological); "dad" = Brian (the Cheesman, step-father).** Thomas's own rule. Watch filenames: `father-*` and the *misnamed* `dad-teaching-bike-riding` are all **Martin → Lakeman**, not Cheesman.
3. **Two menus** — the elaborate **desk menu** and a **plain/simple list** (toggle "View as a plain list", `html.tc-desk-plain`, same `.tc-desk__mobile` markup as the mobile fallback). The plain one is kept to **90vh × 100vw**. Both are BHAG surfaces — plan + propose; offer a ChatGPT brainstorm.
4. **Photo workflow (established this session):** originals stay **off-repo** (Thomas's archive). Heritage-line photos → I optimize with **PIL** (`max 1600px`, JPG q82) into `assets/img/heritage/<line>/`, reference via `{line}-images.json` (`"theme/assets/img/heritage/<line>/x.jpg"` path form), regenerate. General/blog photos → **WP Media** (auto-scaled). The nice batch trick: build an `.xlsx` with embedded thumbnails so Thomas can tag years/notes (see `Heritage research\cheesman-photos-years.xlsx`).
5. **Editorial philosophy** (`Heritage research\Docherty-EDIT-change-log.md`) now has two standing amendments: **preserve migration/geography** when condensing, and the **family/world balance rule** (introduce an era once, then subordinate context to a named person — McIver is the model).

---

## What's next (Thomas's hand-off)

### 1. ⛔ Create the Cheesman "Story" WP page (manual, blocks the long-read)
Title **"Story"**, slug **`story`**, **Parent = The Cheesmans**. Then Purge LiteSpeed. (Thomas does this in wp-admin.)

### 2. ⭐ The Thomas & Melanie pages (next content focus)
The two people all eight lines converge on don't have their own pages yet. Thomas wants to **write the next manuscript(s)** for a **Thomas** page and a **Melanie** page — likely **long-reads**, first-person where appropriate.
- **Photos already on hand for these:** the Cheesman batch leftovers that weren't placed — `me`, `mee`, `me-at-shushwap`, `xmas-decoration`, `aunty-and-uncle`, `visiting-edmonton` (2004), and `mary` (Melanie's dog) — sit in `Heritage research\photos\` (and were optimized/tidied out of the theme). Melanie's own photos to come from Thomas.
- These could either mirror the **kid person-spokes** (`page-patience.php` / `page-daniel.php` / `page-faith.php` → per-person prose) **or** be full long-reads via the same pipeline. Thomas to decide; he's writing the md.
- **Privacy:** 🔒 Thomas & Mel are co-parents (separated, undisclosed). Married 2 July 2016 = stateable historical fact; present tense = **co-parents only**, never framed as a current couple. Melanie's page especially: surface living-person framing for Thomas's call.

### 3. ⭐ Task E — the converging-families map (still the big BHAG, unstarted)
Build spec is written: **[docs/CONVERGING-MAP-SPEC.md](docs/CONVERGING-MAP-SPEC.md)** ("The Lanterns of Record"; D3-geo; one xlsx per family; all paths converge on Alberta). Plan + propose per phase; offer a ChatGPT brainstorm first. The migration detail restored in Task C is exactly the data this visualises.

### 4. Loose ends / optional
- **Rycroft & Steinke have only DRAFT manuscripts** (no `-EDITED`), and **Steinke lives only inside the Rycroft draft** as a sub-section. If a proper EDITED/standalone Steinke is wanted, that's open. Their bodies are generated from the DRAFTs.
- The **4th Martin photo** (`father-18yo-lol`, ~1978, near-duplicate of the early-20s one) was left out of the Lakeman chapter — available if wanted.
- A **hero image / more chapter photos** for the other lines could be added with the same photo workflow whenever Thomas supplies them.
- Balance-audit also flagged: Rycroft's embedded-Steinke paragraph leans context-heavy (fix when records appear).

---

## Carried project rules (see [CLAUDE.md](CLAUDE.md))
- ⭐ **Never auto-commit-and-push.** Propose + wait for "go." Task-scoped only.
- ⭐ **Bump `style.css` Version** per theme commit (Edit tool, not `sed`). Pure-docs commits (like this file) don't bump.
- ⭐ **Never `object-fit: cover`** — always `contain`; author images at the target ratio. (Photo optimization uses `contain`-friendly resizing, never crops.)
- ⭐ **Never `git add .`** — explicit paths only (the `sk_*.html` scrapes must never be swept in).
- ⭐ **Strip `-e<digits>` from WP image URLs;** use the bare stem.
- ⭐ **Never cross-pollute BYR / GPRS / TC** — confirm the site from the path before every edit.
- ⭐ **Desk-menu + both menus + drawer-footer are BHAG surfaces** — plan + propose; offer a ChatGPT brainstorm. Same posture for the Task E map.
- 🔒 **Privacy:** Thomas & Mel are co-parents; status private; don't disclose or frame as a current couple; don't auto-rewrite existing copy that mentions Mel. Don't publish names/photos of living relatives unprompted (cousins' kids' names/birthdays are explicitly off-limits).
- **Commit co-author footer:** `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **LiteSpeed → Purge All** after every push; hard-refresh.
- **Diary CSV:** `C:\Users\thoma\Desktop\My Files\Claude-Diary\diary.csv` — read at start, append a row at wrap.

---

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`). **Start by reading `CLAUDE.md`, `V0.19.md`, `V0.18.md`, and `V0.17.md`** at the repo root — V0.17 documents the heritage long-read *system*, V0.18/V0.19 are current state and the task list.
>
> **State:** all **eight** family lines now have an illustrated long-read; the Cheesman line (Thomas's own, first-person) shipped last session as line 01 of 08, with photos, theme 1.0.434, pushed. Navigation was rebuilt so all eight lines are full peers (numbered 01–08); in-text "Notes" link to the appendix; there's a full breadcrumb trail; the atmosphere now colours per-line; over-cut migration history was restored and a balance pass run. Manuscripts/images/`_md2heritage.js` live off-repo in `C:\Users\thoma\Desktop\Heritage research\`.
>
> **First, confirm** the Cheesman "Story" WP page exists (title "Story", slug `story`, parent The Cheesmans) — the long-read 404s until Thomas creates it, like McIver before it.
>
> **Open tasks (confirm priority with Thomas):** (1) the **Thomas & Melanie pages** — he's writing the manuscript(s); likely long-reads, first-person; leftover Cheesman photos + Melanie's photos go there; handle Mel with the privacy rule (co-parents only). (2) **Task E — the converging-families map** (BHAG; spec in `docs/CONVERGING-MAP-SPEC.md`). (3) optional: an EDITED/standalone Steinke; more hero/chapter photos via the established PIL → `assets/img/heritage/<line>/` → `{line}-images.json` workflow.
>
> **Don't:** `git add .` (pre-existing `sk_*.html` scrapes), bump version with `sed`, use `object-fit: cover`, or auto-commit-and-push (propose first). Remember **"father" = Martin (Lakeman), "dad" = Brian (Cheesman)**.
