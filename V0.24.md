# V0.24 — Trees ×7: the descent-tree rollout lands on every lineage. All eight long-reads now carry a pastel-tinted tree, a pastel-family blob trio, and a per-line WebGL tint/accent pair. **Next thread's headline: the lay-ons when Thomas returns the three DOCXs, and the research chases.**

**Date wrapped:** 2026-06-11
**Theme version at wrap:** 1.0.518 (pushed) · HEAD = `d90fd3b`
**WordPress at wrap:** 7.0 "Armstrong" · **Astra:** 4.13.x

Successor to [V0.23.md](V0.23.md). Next agent writes **V0.25**. V0.23 carries the tree/pastel system's birth and the Steinke breakthrough; this doc is the seven-line rollout.

---

## ⚠️ Read this first — state

- **All theme code committed AND pushed** (TC auto-push standing). 1.0.511 → **1.0.518** across 7 commits, one per line. Remind Thomas: **LiteSpeed → Purge All** — seven deploys landed this thread.
- **The V0.23 headline is DONE:** descent trees live on all seven remaining lineages (Cheesman, Docherty, McIver, Lakeman, Verboom, Rycroft, Haiste), each with its blob trio re-derived in its approved pastel's family and its `body:has` WebGL tint/accent pair. Steinke's "pilot" comment in style.css is retired; the system is now uniform across all eight.
- **Three DOCX passes still with Thomas** (his edits are canonical — do NOT regenerate his copies): `Heritage research\Patience-Page-Text.docx`, `Daniel-Page-Text.docx`, `Cheesman-Ch7-Cooking-Years.docx`. Verdicts to apply at lay-on live in **`Heritage research\Lay-On-Decisions.md`**.
- **🔒 Thomas & Melanie are "parents", NEVER "co-parents".** Every new tree chip uses the approved Steinke wording ("Married **2 July 2016** in Calgary" / "The parents of Patience, Daniel, and Faith").
- **Untracked at repo root, leave alone:** helper scripts (`build-*.py`, `process-daniel-media.py`) and raw clips in `assets/img/thomas/`. **Always `git add` explicit paths — never `git add .`.**
- **Bump `style.css` Version with the Edit tool, never sed/PowerShell.** (This doc is pure docs — no bump.)

---

## What this thread shipped (1.0.512 → 1.0.518)

One commit per line; each = tree authored in the off-repo `{line}-images.json` → body regenerated via `node _md2heritage.js` → blob trio + WebGL pair in style.css → version bump.

| Commit | Ver | Line | Tree | Palette notes |
|---|---|---|---|---|
| `d8c8778` | 1.0.512 | **Cheesman** | 4 gens (John & Sandy → Brian & Maryanne → Thomas & Melanie → kids) | hearth gold `#936D34/#BFA463/#704D29` · tint `#9D7F43` accent `#EEE0BA` |
| `d2625cf` | 1.0.513 | **Docherty** | 9 gens, Inishowen → Grande Prairie | Donegal heather `#85426C/#B36F97/#6A2F5B` · `#954B79/#EABED7` |
| `11b99e8` | 1.0.514 | **McIver** | 8 gens incl. a two-parent-couples row (Norman×Henrietta ‖ Campbell×Cameron) | sea glass `#437F79/#6FAEA5/#2C6868` · `#4E8D87/#C0E7E1` |
| `f6118ff` | 1.0.515 | **Lakeman** | 11 gens, Beemster → Grande Prairie | polder green `#497C46/#78AC72/#2F6534` · `#548B50/#C6E6C1` |
| `6a4c8f5` | 1.0.516 | **Verboom** | 9 gens, mounted on "Where the Verbooms meet the Lakemans" (no Living Line chapter); opens with the 1825-akte two-couple row | Delft blue `#385894/#698BBF/#283D77` · `#4366A3/#BED2EF` |
| `24411ab` | 1.0.517 | **Rycroft** | 8 gens, Leeds → Hawai'i → the hat-draw town | Kona sunset `#994C38/#BB7E68/#703329` · `#A35743/#EFC6B9` |
| `d90fd3b` | 1.0.518 | **Haiste** | 13 gens (the deepest), Yorkshire tannery → Grande Prairie | indigo wash `#4F3F8D/#8271B7/#342C72` · `#5B499C/#CCC0ED` |

**Palette derivation (for future lines/tweaks):** Steinke's ratios were generalized — blobs at L≈39/57/32 in the pastel's hue with tempered saturation, WebGL tint at L≈44, accent at L≈83 (a lighter sister of the pastel). Hex values computed with an HSL→hex script, not eyeballed.

**Verification:** word-diff per regeneration confirmed the converter pipeline stays idempotent (every diff = pure tree insertion); structural counts across all 8 bodies — exactly 1 `<section class="heritage-tree">` each, gens/couples/carriers match the authored data; all 8 jsons parse; 8 `body:has` WebGL pairs in style.css; `initHeritageTreeTilt()`/`initHeritageTreeFacts()` are sitewide and pick the new trees up automatically.

---

## Tiering & privacy decisions worth knowing (per line)

These follow "mine the lineage table + Notes; never invent; tier every fact" and the Steinke privacy patterns (living people first-names only; unnamed living relatives stay unnamed).

- **Cheesman** — the manuscript *declines* a lineage table on purpose (thin line, living people). Its tree is therefore the four household generations the story itself tells, dates-light, first names for the living (Sandy, Brian, Maryanne, David, Christopher, Jonathan — all already on the page). Carrier highlight follows the **name** (the chosen channel), not blood.
- **Docherty** — John & Mary chip says outright they're the speculative floor; the two unread ScotlandsPeople death certificates (1862 Johnstone, 1865 Hamilton) are named in the dossiers as unread. Patrick Andrew carries "*(twins?)*".
- **McIver** — the confessional-wall framing is kept as the geography **inference** the Notes weigh ("by the island geography…"); Norman George's war death rides its CWGC verification; the empty-chair correction (James Roderick × **Mary Ann Birse**, not John Alexander) is the gen-4 chip's story.
- **Lakeman** — numbered generations start at the first datable parish entry (Sijmon Sijmonsz c. 1698); the speculative cloth-trade Lakenmen live in that chip's dossier, not as chips.
- **Verboom** — Hannes stays "**probable** as Suzanna's father" exactly per the Notes; the false-trail Cornelis/Aagje couple stays excluded. Suzanna's chip lights the Verboom side of the same couple the Lakeman tree lights from Rienk's side.
- **Rycroft** — sourced from `Rycroft-Story-DRAFT.md` (this line's only ms). James Robert & Eliza stay probable; the discarded Essex false-ancestry is named as discarded; Eric's born-before-the-1911-marriage wrinkle is set down as the Notes set it down. Sam & Bette's chip cross-links to the Steinke story.
- **Haiste** — **Ernest's wife stays unnamed on the page.** The Alice Maud Smith identification (PROBABLE, per `Haiste-Ernest-Wife-Hunt-Report-2026-06-10.md`) is NOT written in — the manuscript corrections await Thomas's call + the Dan interview. Joseph Jr. is marked the "hypothesis" bridge; the overturned 1757 flax dresser is named as overturned. Dan stays first-name (living).
- **Cross-tree shared chips** — every tree ends with the same two approved chips (Thomas × Melanie "m. 2016"; the kids in Grande Prairie), verbatim from the Steinke pilot. That repetition is the design: the eight trees converge on the same two rows, which is exactly the data spine Task E's converging map will draw from.

---

## Second arc (same thread): the high-contrast atmosphere (1.0.519–520)

Thomas reviewed the trees live and called the next move: the lineage pages should look like **/family/thomas** — vivid line-coloured blobs straight off near-black, no colour wash tinting the dark ("the cool yellow blobs without the blue background above the blobs"). What shipped:

- `8ed9af3` / **1.0.519** — the atmosphere layer's opaque colour-washed base is **gone** (`background: none`); the per-line WebGL canvas (z:-1) is now the background, exactly the Thomasito mechanism. All 8 blob trios + tint/accent pairs re-derived **VIVID** (full-chroma versions of each pastel — the `#ff5a1f/#ffb020` relationship per line). A 3-lens adversarial review workflow drove four pre-commit fixes: blob-b capped at L≈56–58 (screen-blend + `brightness(1.12)` clipped highlights to white), Verboom pushed to 223° and Steinke to ~202° (their washes sat 5° apart), Haiste's tint lifted, glass-plate backdrop `saturate(155%→118%)`.
- `7e5ea6a` / **1.0.520** — **latent cascade bug found by live `getComputedStyle` verification:** the "sensible default" `.heritage-longread` palette block sat AFTER the per-line blocks with equal 0,1,0 specificity on the same element, silently overriding **every** per-line blob trio (the per-line atmospheres likely never rendered as authored — same genre as the 1.0.511 hub-card fix). Default block moved above the per-line palettes; the CSS comment explains why it must stay there.
- Verified live in Chrome on all 8 long-reads at ver=1.0.520 (computed `--blob-a`/`--webgl-tint` checked on-page); **LiteSpeed Purge All clicked via the admin bar**, guest HTML confirmed serving 1.0.520.
- Taste note for Thomas: Verboom + Steinke read moodier than the warm lines (blue carries less luminance per unit of HSL-L; stone is quiet by design). If he wants those two lifted, it's a small palette nudge.

---

## 🩹 Sharp edges this thread

1. **The converter must run from `Heritage research\`** (`cd` there first) — `_md2heritage.js` resolves the images.json relative to cwd, and the Bash tool's cwd resets between calls; use `git -C <repo>` for the verification diffs in the same call.
2. **Word-diff is the right regen check:** `git diff --word-diff=plain <body.php> | grep -oE '\[-.{0,60}'` — if the only removed segment is the insertion-point boundary, the pipeline stayed idempotent. Used on all seven; all clean.
3. **Edit tool on off-repo jsons requires a Read first** (tool guard) — reading via `node -e` doesn't count.
4. The old "Married in Teepee Creek" passport copy is **gone from the repo** (passport page retired) — no venue discrepancy exists; all manuscripts say Calgary, 2 July 2016.
5. Lakeman json key `"Interlude — The Verbooms"` does **not** match the md header (`Interlude — Suzanna's people, the Verbooms`), so its kicker/pullquote never render. Pre-existing, harmless, worth fixing on the next Lakeman touch.
6. **Equal-specificity default blocks must precede their per-line overrides.** `.heritage-longread { --blob-* }` vs `.heritage-longread--{line} { --blob-* }` are both 0,1,0 on the same element — only source order decides. The default sat last and silently won for who-knows-how-long. When adding any new "default + modifier" variable pattern, put the default FIRST, and verify with `getComputedStyle` on the live page, not by eyeballing the colour.
7. **Live-verify with computed styles, not screenshots alone.** The Cheesman page looked "wrong-coloured" for two stacked reasons (stale `?ver` HTML racing the deploy + the cascade bug); `getComputedStyle(...).getPropertyValue('--blob-a')` in the browser console separated them in one call.

---

## What's next (in order)

1. **Lay-ons when Thomas returns the DOCXs:** Patience, Daniel (verdicts in `Lay-On-Decisions.md`), Cheesman Ch 7 cooking years (then: describe-sheet for the 28 culinary photos' captions if he wants them, and possible Ch 7 illustration).
2. **Research chases** (V0.23 lists them): Bette's 1959 marriage record, the Gouchey family, SGGEE film 1895621/1, Michał's ~1827 baptism, the 1901 S.S. Pennsylvania manifest; Haiste — the one FreeBMD lookup that makes Alice Maud Smith airtight, then the manuscript corrections on Thomas's call. The **Dan interview** (`Dan-Interview-Questions.docx`) feeds four pages.
3. **Carried:** Melanie's page (draft id 2470); comments + replies; retro-links to `/family/thomas`; **Task E converging map** (`docs/CONVERGING-MAP-SPEC.md`) — the eight trees are now its data spine, colours settled in §16; census refresh.

---

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`). **Start by reading `CLAUDE.md` and `V0.24.md`** at the repo root. WordPress 7.0; skim `docs/WORDPRESS-7.0.md` before theme work.
>
> The descent trees are live on all eight heritage lineages (V0.24 has the rollout record and per-line tiering decisions). **The headline now: lay-ons when Thomas returns the three DOCXs** (`Patience-Page-Text.docx`, `Daniel-Page-Text.docx`, `Cheesman-Ch7-Cooking-Years.docx` — check LastWriteTime before touching; verdicts in `Heritage research\Lay-On-Decisions.md`), **then the research chases** (Steinke/Gouchey/Haiste lists in V0.23 §"The Steinke research file").
>
> **Hard rules:** Thomas & Melanie are **parents, never "co-parents"**; never invent facts, tier every claim; bump `style.css` Version with the Edit tool only; explicit `git add` paths; never `object-fit: cover`; LiteSpeed Purge All after pushes.
