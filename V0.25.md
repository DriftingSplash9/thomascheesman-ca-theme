# V0.25 — Consolidation: the build mountain is behind us. The heritage system (trees · pastels · film reels) and the Lanterns of Record map shipped complete in the V0.24 marathon; what remains is **content to add and edit, a few odd ends, and the external review cycle.**

**Date wrapped:** 2026-06-11
**Theme version at wrap:** 1.0.530 (pushed) · HEAD at the V0.25 commit
**WordPress at wrap:** 7.0 "Armstrong" · **Astra:** 4.13.x

Successor to [V0.24.md](V0.24.md) — read it for the nine-arc build record (trees ×7, vivid atmospheres, film reels, map P0→P3 + two live review rounds, menus). Next agent writes **V0.26**.

---

## ⚠️ Read this first — state

- **All theme code committed AND pushed**; LiteSpeed purged at every deploy this session. Remind Thomas to **Purge All** after any future push.
- **The site is feature-complete for this era.** Eight long-reads with descent trees, vivid per-line atmospheres, two film-reel video embeds, and the converging-families map live at **`/family/heritage/map/`** (canonical — Thomas reparented it under Heritage; `/map/` 301s). The Map is in the plain + mobile menus; its **desk-menu OBJECT placement is still Thomas's to decide** (BHAG surface — offer the ChatGPT brainstorm prompt when he picks it up).
- **Three DOCX passes still with Thomas** (his copies are canonical — never regenerate): `Patience-Page-Text.docx`, `Daniel-Page-Text.docx`, `Cheesman-Ch7-Cooking-Years.docx`. Apply verdicts from `Heritage research\Lay-On-Decisions.md` at lay-on time.
- **🔒 Thomas & Melanie are "parents", NEVER "co-parents".** Living relatives: first names only, nothing unpublished.
- **Bump `style.css` Version with the Edit tool only; `git add` explicit paths only.**

---

## The systems a new agent must know (one paragraph each)

1. **Heritage long-reads:** `{Line}-Story-EDITED.md` (+ Rycroft DRAFT) in `Heritage research\` → `node _md2heritage.js <md> <out.php> "<Label>" <json>` (run FROM that folder) → `inc/heritage/{line}-story-body.php`. Labels: Lakeman=`"Lakeman"`, Verboom=`"Verboom"`, rest `"The X"`. The `{line}-images.json` carries kickers, pullquotes, figures, ribbons, **`tree`** (descent tree) and **`video`** (lite YouTube film reel) keys. Verify regens with `git diff --word-diff=plain` — insertion-only diffs prove idempotency.
2. **The map pipeline:** `Heritage research\family-map-data.xlsx` (197 pre-seeded rows + GAPS sheet) → `python _xlsx2map.py` → `inc/data/family-map.json` → commit + push. New/edited rows (Melanie's Edmonton years, Dan's points, coordinate fixes) flow automatically: amber rows = TBC. Engine: `assets/js/family-map.js` (D3, only on the map page), `page-map.php`, `assets/css/family-map.css`, 50m land at `assets/data/world-50m.json`. Undated rows kindle at their **person's** first dated record.
3. **Film reels:** `video` key in images.json → facade only (zero YouTube bytes until click) → `initFilmReels()` swaps in youtube-nocookie. Channel: **Drifting Splash VIV** (`@DriftingSplash9`). Haiste `GVPemTqCjf8` · Lakeman `T1_9Wto-7WU`.
4. **The eight pastels** (canonical comment at the long-read `--line-color` block in style.css) drive everything line-coloured. Vivid derivations power the blobs/WebGL atmospheres; **default-palette blocks must precede per-line overrides** (the 1.0.520 cascade lesson).

---

## What's left (the consolidated queue)

### Content (the bulk)
1. **Lay-ons when the three DOCXs come back** — Patience, Daniel (verdicts recorded), Cheesman Ch 7 cooking years (then the 28-photo caption describe-sheet if he wants captions, and possible Ch 7 illustration).
2. **Melanie's page** — WP draft id 2470 waits; both of her lines are now fully documented. Likely the person-spoke idiom (Patience/Daniel/Faith pattern).
3. **Map data fills** — Melanie's Edmonton years + return-to-GP (she's being asked); **Dan's life points** ("his story is incomplete!") via the Dan interview (`Dan-Interview-Questions.docx` is ready); the GAPS-sheet items. Each fill = xlsx row → pipeline → push.
4. **Research chases** (V0.23 §"The Steinke research file"): Bette's 1959 marriage record; the Gouchey family; SGGEE film 1895621/1; Michał's ~1827 baptism; the 1901 S.S. Pennsylvania manifest; **Haiste:** the one FreeBMD lookup that makes Alice Maud Smith airtight, then the manuscript corrections on Thomas's call (Ernest's wife is UNNAMED on the page until then).
5. **Retro-links** — /about, /hcs, and the Cheesman long-read → /family/thomas.

### Odd ends
6. **The external review cycle — round 1 TRIAGED (1.0.531).** Grok reviewed (A- overall, mostly blind: lots of "likely/presumed"); GPT honestly declined without browsing access. Verified against the live site before acting. **Already covered** (no action): AIOSEO emits OG/Twitter + BreadcrumbList/WebPage/WebSite schema + the sitemap; the theme has a hand-built Person schema; Hostinger Tools generates `/llms.txt`; reduced-motion handling is comprehensive; images already optimized. **Shipped:** hand-written meta descriptions for all 26 pages via the `aioseo_description` filter (keyed by `get_page_uri()`, versioned in functions.php — every page previously had NONE; AIOSEO mirrors the copy into og:/twitter:description); VideoObject JSON-LD beside each film reel (converter `videoHTML()`, new `iso` duration key; uploadDate never invented). **Needs Thomas (2 minutes in wp-admin):** trash duplicate page **id 3920** ("map", slug `map-2` — pollutes llms.txt + sitemap); set a default social **og:image** in AIOSEO → Social Networks (recommend a wide family or desk photo). **Still open from the review:** GPT's deep review if wanted — the agent can capture a screenshot pack from the browser for it; print stylesheet (real, archival-fitting, medium effort); a Lighthouse/axe pass.
7. **Desk-menu object for The Map** — Thomas deciding; ChatGPT brainstorm offer stands.
8. **Comments + replies** (comment-on-comment) — carried.
9. **Zero-glow escape hatch** — if Thomas decides the map dots should lose the glow entirely after living with the tightened version: gradient → plain fill, 2 lines.
10. **Lakeman json Interlude key mismatch** (`"Interlude — The Verbooms"` ≠ md header) — its kicker never renders; fix on next Lakeman touch.
11. **Carried long-tail:** scrapbook page (design complete since V0.07, one TBD: wooden-desk surface URL); census/word-count refresh; map chapter-anchor deep links; Campbell/Cameron map-thread shades are a P-whatever render call if Thomas wants them tuned.

---

## 🩹 Sharp edges (inherited + this thread — the permanent ones live in V0.24)

- Converter runs from `Heritage research\`; `git -C` for repo commands in the same call.
- Verify colours/positions with **live `getComputedStyle`**, not screenshots alone (stale `?ver` HTML races deploys).
- Chrome-MCP scroll bypasses DOM wheel handlers — test wheel features by dispatching real `WheelEvent`s in page context.
- Admin-bar Purge All: **screenshot-confirm the dropdown is open before clicking** — a blind click can land on a page link.
- d3 zoom: clip-path on an unscaled wrapper; screen-space offsets INSIDE counter-scaled groups; clamp rAF dt (hidden tabs).
- A sitewide button rule paints button text with transparent `-webkit-text-fill-color` — set `text-fill-color: currentColor` on any new button class.

---

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`). **Start by reading `CLAUDE.md` and `V0.25.md`** at the repo root (V0.24 holds the deep build record if needed). WordPress 7.0; skim `docs/WORDPRESS-7.0.md` before theme work.
>
> The build era is done — eight long-reads with descent trees, per-line atmospheres, film reels, and the Lanterns of Record map are all live. **The work now is content and polish:** lay-ons when Thomas returns the three DOCXs (check LastWriteTime first; verdicts in `Heritage research\Lay-On-Decisions.md`), Melanie's page (draft 2470), map-data fills via `family-map-data.xlsx` → `_xlsx2map.py`, the research chases, and triaging the external review (`docs/SITE-REVIEW-PROMPT-2026-06.md`) when Thomas brings back the report.
>
> **Hard rules:** Thomas & Melanie are **parents, never "co-parents"**; never invent facts, tier every claim; bump `style.css` Version with the Edit tool only; explicit `git add` paths; never `object-fit: cover`; LiteSpeed Purge All after pushes; desk menu + drawer footer are plan-first surfaces.
