# V0.28 — The content "breathe / cut-sensitive / de-duplicate" pass: the **three kid pages got a privacy edit**, and the **heritage lineage-table caption was de-duplicated**. The wholesale rewrites are parked; surgical edits are the approach. **⭐ The next task Thomas named is REDOING THE KEEPSAKE PDFs with images inline.**

**Date wrapped:** 2026-06-14
**Theme version at wrap:** 1.0.562 (pushed) · HEAD = `fbd5b54`
**WordPress at wrap:** 7.0 "Armstrong" · **Astra:** 4.13.x
**Live:** https://thomascheesman.ca

Successor to [V0.27.md](V0.27.md) (the keepsake-PDF system + the rewrite-batch handoff) and [V0.26.md](V0.26.md) (the whole-project map — still the best single overview). Next agent writes **V0.29**.

---

## ⚠️ Read this first — state

- **All theme code committed AND pushed** (TC auto-push per CLAUDE.md rule 1). This thread: **1.0.559 → 1.0.562**. **Remind Thomas: LiteSpeed → Purge All.**
- **The plan pivoted.** The wholesale `revised_*.md` rewrites from V0.27 (that would have *replaced* the long-reads with short versions) are **PARKED** — Thomas said "ignore those for now." Instead he supplied **surgical edit-suggestion files** and we adopted his framing: **let the content breathe, cut sensitive kid details, eliminate duplicity.** Both batches below are that approach.
- **🔒 Kid-privacy is now actively being applied** (not just flagged). Living relatives first-names-only; parents never "co-parents." When editing prose, **generalising only removes detail — never invent** (the standing voice-pass rule).
- **Heritage edits go through the off-repo pipeline:** edit `{Line}-Story-EDITED.md` (Rycroft & Steinke are `-DRAFT.md`) → `node _md2heritage.js <md> <out.php> "<Label>" <json>` from inside `C:\Users\thoma\Desktop\Heritage research\` → **word-diff the regenerated body** to confirm the converter stayed idempotent. Kid/person pages are **hand-written PHP** (edit `page-*.php` directly; they use **straight apostrophes**, the heritage bodies use `&rsquo;`).

---

## ⭐⭐ THE NEXT TASK — redo the keepsake PDFs with images INLINE

Thomas's explicit next step: **"redoing the PDFs"** so they **include the images inline.** Context from his earlier direction: *"the heritage files can have the old images added back in, keep the kids' just text for now."*

So the redo is **asymmetric**:
- **Heritage PDFs (8 lineages): images back, inline.** The figures already live on the live heritage pages (keyed via `{line}-images.json`), so nothing new to source — just stop hiding them in print and let them embed.
- **Kid PDFs (Patience/Daniel/Faith): stay TEXT-ONLY** until Thomas selects new photos *with the kids' permission*. (Thomas's own page can take its inline figures.)

**Where the levers are (all from V0.27's keepsake-PDF system):**
1. **`assets/css/print.css`** currently hides *all* photos: `#primary figure, #primary img { display:none }` (§8). To bring heritage images back while keeping kids text-only, **scope the hide to the person pages** — i.e. hide figures only under `.person-spoke` (kids), and **restore the figure layout for `.heritage-longread`** (the centred-plate styling + captions). **The original figure CSS is in git history** — see commit `8a97c65` ("drop photos…") which *replaced* the figure-layout block with the hide; revert that block for heritage and re-scope.
2. **`build-keepsake-pdfs.py`** (repo root, gitignored) has **`EMBED_IMAGES = False`** — flip to **`True`** to re-enable the Pillow downscale-and-embed pass (1800px / JPEG q82; it exists precisely for this). Heritage line photos are already PIL-optimised, so files should stay modest.
3. **Regenerate + verify** with PyMuPDF (no poppler — the Read tool can't open PDFs): render pages to PNG, and re-run the existing fitz checks (per-page transparency-group count ~0; per-line worst title→kicker gap ≥ +5px — the print bugs from V0.27 are all fixed, just don't regress them).
4. **Then** the on-site **Download buttons** (still unwired): host the finished PDFs in WP Media (Thomas uploads), link lineage buttons publicly; kids' held until the family login.

**Watch:** re-showing figures will reintroduce the page-break/figure-fit behaviour — sanity-check that a tall portrait doesn't blow out a page and that figures don't collide with the chapter-heading fixes from V0.27.

---

## This thread's work (the content pass)

**Batch 1 — kid privacy pass** (1.0.560–561, commits `b7e2088`, `f7e4cbd`). Generalised the genuinely-sensitive specifics across all three kid pages, in Thomas's voice, his option-calls applied:
- **Faith** — exact DOB + weight/length dropped; the birth breathing-emergency softened (kept the terror, Melanie wheeling herself in on sheer will, the "have faith" turn; dropped the CPR/stairs/staff-count play-by-play).
- **Daniel** — DOB + hospital (QE2) + build dropped; C-section explainer cut; tongue-tie/pumping → "feeding was its own long battle"; **full legal name no longer spelled out** (namesake meaning kept); subtitle "constellation **on his face**" → "of his own"; added a nod to **uncle JP (Jonathan)** on the Man Hut.
- **Patience** — birth dates + emergency-caesarean detail dropped (patience theme + the Ric's Grill beat kept); feeding softened; "twelve going on sixteen / sleepovers / TikTok" → timeless; "the weight she carries" softened (the loud-bang-means-Dad's-hurt line removed); HCS "gamble" opener **kept** (his call); **Kristi-Ann** fixed (the friend was **Kristi**; Kristi + Ann-for-Maryann).

**Batch 2 — heritage de-duplication** (1.0.562, commit `fbd5b54`). Only the **lineage-table caption** was still a verbatim echo — identical across three lines. Varied each to its own theme via the manuscript→regen pipeline (word-diff confirmed caption-only change):
- **Lakeman** → "Four centuries of *doorkomen* … come to rest at the foot of this table, in three Grande Prairie children …"
- **Rycroft** → "The line that reached from a Leeds back-street to Honolulu and home again ends, for now, at the foot of this table …"
- **Haiste** → "Thirteen generations after a Yorkshire tanner kept his household alive through a civil war, the Haiste thread comes to rest …"
- **The other 5 (Cheesman / Docherty / McIver / Steinke / Verboom) were verified and LEFT** — V0.26 already gave each a distinct prose ending (Cheesman "been here before", Docherty "It will again", Steinke "what *durchhalten* was for", McIver "the islands come ashore", Verboom is now the records appendix). No verbatim echo remained. The descent-tree node "Heirs of all eight family lines — the confluence this whole site is built around" repeats across all 8 by design — **Thomas said leave it.**

---

## The rewrite plan — status

The collection Thomas is building (the keepsakes as one work): **a frame (Thomas) + 8 ancestral lines + 3 kid portraits**, bound by an Introduction and a Conclusion. Status:
- **Batch 1 (kids privacy)** — ✅ done.
- **Batch 2 (heritage de-dup)** — ✅ done.
- **Batch 3 — Thomas's own page** — **NOT DONE.** Light: apply the privacy lens only where the kids are named on `page-thomas.php` (the edit-suggestion file `thomas_edit_suggestions.txt` in `Downloads/`, summarised: soften the named-children legacy framing; leave the HCS/advocacy material as-is).
- **The Introduction** — a draft exists ("For Patience, Daniel, and Faith / Eight lines run down the centuries…", in `Downloads/collection_structure_and_intro.txt`); slot it in once the edits settle. Proposed reading order in that file: Thomas → the 8 lines (01–08) → the 3 kids as a closing triptych.
- **The Conclusion** — **Thomas is writing it himself** ("to bind everything loose and send it to the future").
- **Staged inputs:** the parked wholesale rewrites + the `*_full.txt` reference extracts are in `Heritage research\_online-rewrites-2026-06\`. The 12 `*_edit_suggestions.txt` + `collection_structure_and_intro.txt` are in `Downloads\` (Thomas said the edit-suggestion files weren't strictly needed — the actionable guidance is "breathe / cut sensitive / de-dup").

---

## Carry-forward backlog (from V0.26/V0.27 — still open)

- ⭐ **Melanie's page** (unbuilt since V0.19, longest-carried).
- ⭐ **Family login (~10 people)** to gate the kid **galleries** (render calls removed, `inc/gallery-*.php` data files kept — one-line restore each). WordPress can do this natively; surface options before custom-building.
- ⭐ **Comments + replies** (carried since V0.20, never started); **GEDCOM downloads + a Dochertys print book** (the keepsake-PDF system is a head-start).
- Research spine: **ScotlandsPeople** (McIver/Docherty) and the **Dan ("Papa Dan") interview** (feeds Haiste + the map).
- Map data fills + the map's desk-menu object (BHAG). Retro-links `/about` `/hcs` → `/family/thomas`. About-page lay-on. Default `og:image`.

---

## 🩹 Sharp edges (reaffirmed this thread)

- **Heritage pipeline:** the converter reads `_image-dims.json` by relative path → run it with cwd = `Heritage research\`. `LABEL` only sets a header comment (`Lakeman` / `The Rycrofts` / `The Haistes` for these three) — match the existing header to avoid a spurious diff. After regen, `git diff --word-diff=plain` to confirm caption-only change; the **"cross-link phrase NOT FOUND: the Verboom story" warning on Lakeman regen is harmless** (V0.26). Match edit-anchors on apostrophe-free spans (the converter smart-quotes `'`→`&rsquo;`).
- **Kid pages use straight apostrophes**, heritage bodies use `&rsquo;` — verify with a grep before constructing Edit `old_string`s.
- **PDF verification needs PyMuPDF** (`fitz`), not the Read tool (no poppler). Render to PNG; the print-layout bugs (transparency-group heading cover, ribbon/table page-scaling, title/kicker overlap) are all fixed in print.css — don't regress them when re-enabling figures.
- **Explicit `git add` paths** (the tree carries `.docx`, `.xlsx`, raw `.mp4` clips, loose `build-*.py`). **Version bump = Edit tool only.**

---

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`). **Read `CLAUDE.md`, then `V0.28.md`, then `V0.27.md` and `V0.26.md`** at the repo root — V0.28 is the latest session + the active task, V0.27 has the keepsake-PDF system internals, V0.26 is the whole-project map.
>
> **The task Thomas named is redoing the keepsake PDFs with images inline** — heritage lines get their figures back (already on the pages, keyed via images.json), kids stay text-only until he picks new photos with their permission. The levers: un-hide/re-scope figures in `assets/css/print.css` (currently `#primary figure,#primary img{display:none}` — restore the figure layout from git commit `8a97c65` for `.heritage-longread`, keep hidden for `.person-spoke`), flip `EMBED_IMAGES=True` in the gitignored `build-keepsake-pdfs.py`, regenerate, and verify with PyMuPDF (no poppler). Then wire the WP-Media download buttons (lineages public, kids held for the family login). Confirm scope with Thomas first.
>
> **Also open:** Batch 3 (light privacy pass on `page-thomas.php` where the kids are named), placing the collection Introduction, and Thomas's own Conclusion (he's writing it).
>
> **Hard rules:** parents never "co-parents"; kid-privacy; generalise = remove detail, never invent; bump `style.css` Version with the Edit tool only; explicit `git add`; never `object-fit: cover`; LiteSpeed Purge All after pushes; heritage edits go through the manuscript→`_md2heritage.js` regen with a word-diff check; verify PDFs with PyMuPDF.
