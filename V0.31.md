# V0.31 — the **audit + privacy-lockdown** session: a $20k-grade website audit delivered (`Website-Audit-thomascheesman-2026-06-17.docx`), then **OD-1 executed end-to-end** — the three kids' pages gated behind the family login + noindexed, their full legal names + birth dates/years stripped from every public heritage story, their photos gated, and the keepsake PDFs regenerated clean. Plus audit quick-wins **SEC-1**, **SEO-2**, **AEO-1**. **⭐ Next: the 3 kid keepsakes need an authenticated regen (login provided), then the audit's Short-Term tier (IA-1 first).**

**Date wrapped:** 2026-06-18
**Theme version at wrap:** 1.0.602 (pushed) · HEAD = `9739acf`
**WordPress:** 7.0 · **Astra:** 4.13.x · **Plugins:** 5 (AIOSEO, Converter for Media, Hostinger Tools, LiteSpeed Cache, MCP Adapter)
**Live:** https://thomascheesman.ca

Successor to [V0.30.md](V0.30.md) (sitemaps + Brevo removal + G7/G9) and [V0.29.md](V0.29.md) / [V0.26.md](V0.26.md) (audit + whole-project map). Next agent writes **V0.32**.

---

## ⚠️ Read this first — state

- **The full audit lives at repo root:** `Website-Audit-thomascheesman-2026-06-17.docx` (untracked — a deliverable, not theme code). 18 categories + 3 deliverables + a weighted score (**79/100 today, ~92 after the roadmap**). The "comprehensive audit TODO" below is the actionable distillation.
- **All theme code committed AND pushed** (TC auto-push, CLAUDE.md rule 1). This thread ran **1.0.594 → 1.0.602** across 6 commits: `c24c1ce` (SEC-1+SEO-2), `1e4f155` (gate kid pages+noindex), `a8abcd5` + `6afe152` (heritage redaction), `e0473a1` (gate kid photos), `9739acf` (AEO-1 portrait).
- **⭐ noindex on the kid pages is set in AIOSEO per-post meta (DB), NOT git.** AIOSEO overrides WP core's `wp_robots`, so the `wp_robots` filter in `functions.php` is only a fallback. If AIOSEO settings ever reset, re-apply noindex on pages 2468/2463/2465 (or via the `aioseo-posts/seo-data-update` ability — that's how it was set). See [[project_tc_od1_privacy_lockdown]].
- **⭐ The heritage `*-story-body.php` are "auto-generated, never hand-edit" — but were hand-edited this session** (surgical, script + dry-run) to strip the kids' PII. Reason: regen via `_md2heritage.js` loses the ribbon + family-tree, and Rycroft/Steinke have **no manuscript**. The Docherty/Haiste/Lakeman/McIver manuscripts were synced. **Don't regenerate casually; if you must, re-apply the kid redaction after.**
- **Self-purge** (`tc-portfolio/purge-cache`) after each push — won't clear combined CSS/JS (Toolbox does that).

---

## ⭐⭐ THE NEXT NAMED TASK

**Regenerate the 3 kid keepsake PDFs (patience/daniel/faith) — needs an AUTHENTICATED render.** Their pages are now fully gated, so `build-keepsake-pdfs.py` (which loads the live site anonymously) only captures the "kept for family" notice. Thomas provided a least-privilege **Family account** for this — username `mglakeman` (password was shared in chat on 2026-06-18, **NEVER commit it**). The generator needs a one-time wp-login.php cookie step added (Playwright: POST to `/wp-login.php` with the creds → reuse the storage state → then render `/family/patience|daniel|faith`). Output to `keepsake-pdfs/`, verify no breakage, hand to Thomas to upload. Until then their download buttons simply stay hidden (no broken state).

> Also still open (Thomas's manual step): **upload the 9 clean public keepsakes** in `keepsake-pdfs/` (cheesmans, dochertys, haistes, lakemans, mcivers, rycrofts, steinkes, thomas, verbooms) to WP Media, deleting any old PII-bearing copies (incl. `-1`/`-2` suffix versions). The old public PDFs embedded the kids' full names + birth dates (confirmed); the buttons resolve to the newest matching upload.

---

## This session's work

### The website audit (delivered)
A consulting-grade audit of thomascheesman.ca across 18 categories, built from a live public crawl + the local theme files + the WordPress Abilities API (plugin/page inventory). Exported to `Website-Audit-thomascheesman-2026-06-17.docx` (docx-js generator in a scratch dir, not the repo). Score **79/100**, ceiling **~92** after the roadmap. The two findings that dominate: **minors' PII on indexed pages (Critical)** and **no preservation/succession plan**.

### OD-1 / PRIV-1 — kids' privacy lockdown (done, verified live)
Thomas's decisions: gate the kid pages entirely; first names only on anything public; keep first names but strip full legal names + birth years; gate the kids' photos. What shipped:
1. **`page-patience|daniel|faith.php`** — whole article wrapped in `tc_user_is_family()`; logged-out get a first-name hero (neutral subtitle) + the gate notice (`tc_render_family_gate_notice($name,'page')`, new `'page'` context in `inc/family-login.php`). **noindex** via AIOSEO (above).
2. **Heritage redaction** — full legal names + birth dates/years stripped from dochertys/haistes/lakemans/mcivers/rycrofts/steinkes story bodies + the rycrofts short spoke + `/family/thomas`; first names only. **Grandmother Lana Lyn Rycroft (1961–2015)** kept (adult ancestor, Faith's namesake). Off-repo manuscripts synced.
3. **Kid photos gated** — `/about` (3 "<kid> and me"), Rycrofts 2×2 grid, Lakemans (baby Patience, Daniel), Haistes (newborn). Ancestor photos + the headstone stay public.
4. **Keepsakes** — the public PDFs were generated pre-redaction and still embedded the PII (confirmed via pdftotext). Regenerated all 9 public ones clean; kid keepsakes deferred (see Next Named Task). Verified **0 PII** across all 7 live pages + the regenerated PDFs.

### Audit quick-wins (done, verified)
- **SEC-1** — the `tc-portfolio/create-page` ability now defaults `status` to **draft** (was publish), for both the unset and invalid cases. `inc/agent-abilities.php`.
- **SEO-2** — `/privacy/` had no meta description; added a hand-written one to the `aioseo_description` map. `functions.php`.
- **AEO-1** — added a **portrait** (`image`) to the homepage Person JSON-LD. `inc/security-and-seo.php`. Bare-stem URL (no `-e<digits>` suffix). Verified live.
- **OD-5** — confirmed **Bare Your Rare canonical = `.org`** (`.com` → `.org`; `.ca` dropped, won't buy). Site schema/`llms.txt` already correct.

---

## ⭐ COMPREHENSIVE AUDIT TODO

### ✅ Done (this session)
SEC-1 · SEO-2 · AEO-1 · OD-1/PRIV-1 (full) · OD-5.

### Owner decisions still open (Thomas)
- **OD-2 — MCP Adapter dormancy.** When the site is "done"/unattended: deactivate the MCP Adapter plugin + revoke the Claude-Agent app password. Not per-session; a late-stage switch.
- **OD-3 — steward succession + a 1-page editorial/privacy charter** (who may be named, at what age; how sources are tiered). Makes the site family-maintainable + inherits the privacy standard. Planning item.
- **OD-4 — citation-depth standard + GEDCOM export.** Add locatable reference handles (repo/collection/akte numbers) per Verified/Probable claim; export a GEDCOM. Ongoing content work. (100-year verifiability.)
- **OD-6 — preservation + succession plan.** Domain auto-renew, off-host archival routine, Internet Archive submissions, named heir. The #1 longevity gap.
- **SEC-2 — drop `'unsafe-eval'` front-end only.** Must keep it for `/wp-admin/` (WP's underscore/Backbone templates need it — proven). Scope: a relaxed `wp-admin/.htaccess`, owner-tested (Media Library + block editor). `.htaccess` is server-only (hPanel). See [[project_tc_csp_htaccess_authoritative]].

### Claude tasks pending (executable; ~ effort)
- **IA-1** (~1 hr) — add the 3 orphan lines (McIvers/Verbooms/Steinkes) to the `/family/heritage/` hub so all 8 lines are discoverable from the hub. **Recommended next — self-contained, no input needed.** `page-heritage.php` / `inc/heritage/`.
- **A11Y-1** (~3–4 hr) — text alternatives for the CSS-`background-image` hero/portrait images (hub + landing pages). Fixes WCAG 1.1.1 + unlocks image SEO. Cross-cuts §1/§6/§10 of the audit.
- **A11Y-2** (~3–5 hr) — focus-trap + return-focus on the desk-menu overlay; a keyboard-reachable text equivalent for the Lanterns map. (BHAG surface — plan/propose for the desk menu.)
- **A11Y-3** (~2 hr) — axe-core + manual NVDA/VoiceOver pass; fix contrast (`--text-light:#94A3B8` on dark risks <4.5:1).
- **PERF-1** (~1–2 hr) — self-host Italiana/Fraunces/Caveat (`font-display:swap`); drop the `fonts.googleapis.com` enqueue + its preconnect. (Pairs with the parked PhotoSwipe/Matter self-hosting.)
- **PERF-2** (~30 min) — verify LiteSpeed page-cache on the deep heritage URLs; the audit saw a `Cache-Control: no-store,private` vs `X-LiteSpeed-Cache-Control: public` conflict on `/family/heritage/` (one cold observation — confirm before acting).
- **PERF-3** (~30 min) — PSI/CrUX baseline on `/` and `/hcs/` (LCP/CLS/INP).
- **SEO-3** (~15 min) — delete the no-op `tc_fix_robots_sitemap()` in `inc/security-and-seo.php` (AIOSEO serves its own robots.txt, so the filter never fires) or move the canonical sitemap line into AIOSEO.
- **CODE-1** (~1 hr) — extract the title/description/social-image maps out of the 1,194-line `functions.php` into `inc/seo-overrides.php` (no behavior change).
- **TRUST-1** (~1 hr) — surface "Last updated / sources reviewed" dates on the long-reads from the existing `Article` `dateModified`.
- **RISK-1** (~2–3 hr) — static-HTML export of all public pages + media, archived off-host; submit key URLs to the Internet Archive (the first real preservation copy; supports OD-6).
- **MOB-1** (joint) — real-device pass (iOS Safari + Android Chrome) on the four interactive surfaces (desk menu, map, pinball, arcade).

### Keepsake follow-up
- **Upload** the 9 clean public keepsakes (Thomas) — see Next Named Task box.
- **Regenerate** the 3 kid keepsakes authenticated (Claude) — the Next Named Task.

### Deferred to late-stage (Thomas's call)
- **Protected media files.** The login gates *rendered HTML + indexing*, but gated media (kid gallery photos, the family-only kid keepsakes) are still fetchable at their direct `/wp-content/uploads/...` URLs by anyone who has the link. True lockdown needs protected-uploads (rewrite rules / a media-gating layer). Thomas deferred this to when the site is more complete.
- **YouTube documentaries** ("Haiste Family Line From Daniel On", "The Lakeman Branch") feature the kids and are public on Thomas's channel + named on `/about`. Left as-is by his decision (the kids' info in them is incidental).

---

## Carry-forward backlog (from V0.30 — still open)
Melanie's page (longest-carried); kids'-page private comments + replies (build on the family login); GEDCOM downloads + a Dochertys print book; ScotlandsPeople (McIver/Docherty) + the "Papa Dan" interview; map data fills + the map's desk-menu object; the collection Intro + Thomas's Conclusion; HCS FAQ (`FAQPage` schema). Thomas's manual items: delete any still-deactivated plugins; image cache TTL; off-host backups; press/society outreach drafts; reciprocal links across the three sites.

---

## 🩹 Sharp edges (new this thread + reaffirmed)
- **⭐ Privacy is now load-bearing:** kids' identifying info (full legal names, birth dates/years) must stay off public/indexed pages; first names are fine. Keep it when adding heritage content or regenerating story bodies. [[project_tc_od1_privacy_lockdown]].
- **⭐ noindex lives in AIOSEO (DB), not git** — the `wp_robots` code filter is a fallback only (AIOSEO wins).
- **⭐ Heritage story bodies were hand-edited** (against the "never hand-edit generated" rule) because regen would lose the ribbon/tree + some lines have no manuscript. Re-apply the kid redaction if ever regenerated.
- **Family login gates HTML, not files** — direct `/wp-content/uploads/...` URLs are still public (deferred hardening item above).
- **WP image URLs:** strip the `-e<digits>` edited-thumbnail suffix — but this session both the `-e` and bare-stem variants returned 200, while `-scaled` 404'd. **Always HEAD-check** (`curl -sI`) and prefer the bare stem.
- **CSP is a static `.htaccess` block, not the PHP header** (Hostinger strips PHP-set CSP); nonce CSP impossible here; dropping `'unsafe-eval'` site-wide breaks wp-admin. [[project_tc_csp_htaccess_authoritative]].
- Standard: LiteSpeed purge after deploy; **explicit `git add` paths** (tree carries `.docx`/`.xlsx`/`.mp4`/loose `build-*.py`/`keepsake-pdfs/` that must never be swept in); bump `style.css` `Version:` with the **Edit tool** on theme-code commits (docs don't bump); never `object-fit/background-size:cover` (use `contain`); "parents" never "co-parents"; secrets entered by Thomas only; auto-push is **TC-only**.

---

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`). **Read `CLAUDE.md`, then `V0.31.md`, then `V0.30.md` and `V0.26.md`** — V0.31 is the latest (full website audit + the OD-1 kids'-privacy lockdown + SEC-1/SEO-2/AEO-1), V0.26 is the whole-project map. The full audit is `Website-Audit-thomascheesman-2026-06-17.docx` at repo root; its actionable distillation is the "Comprehensive Audit TODO" in V0.31.
>
> **Purge cache yourself** via `tc-portfolio/purge-cache` after each push. `tc-portfolio/list-plugins` inventories plugins (5).
>
> **⭐ Next named task:** regenerate the 3 kid keepsakes with an *authenticated* render (Family login `mglakeman` — password is in the 2026-06-18 chat, never commit it; add a wp-login.php cookie step to `build-keepsake-pdfs.py`). Thomas also needs to upload the 9 clean public keepsakes in `keepsake-pdfs/`. Then the audit Short-Term tier — **IA-1 first** (orphan lines on the heritage hub).
>
> **⚠️ Privacy:** kids' full names + birth years stay off public/indexed pages (first names OK); noindex on the kid pages lives in AIOSEO (not git); the heritage story bodies are hand-edited for redaction (don't regenerate without re-applying it).
>
> **Hard rules:** plan-and-propose on BHAG surfaces (desk menu, drawer); kid-privacy; "parents" never "co-parents"; bump `style.css` Version (Edit tool) on theme-code commits; explicit `git add`; never `object-fit/background-size:cover`; CSP is `.htaccess`-only (don't re-attempt nonce CSP); secrets entered by Thomas only; auto-push is TC-only.
