# V0.34 — the **AI-SEO + audit-cleanup** session: shipped the HCS FAQ (visible accordion + `FAQPage` schema), enriched author **E-E-A-T** schema on the cited essays, wired the long-promised **retro-links** to `/family/thomas`, ran the **PERF-3** baseline and the **A11Y-3** automated pass (axe-core, one minor finding fixed), and labelled the last two unlabelled desk-menu inputs. Plus a grounded **reassessment of Google AI-SEO** that reframes the real lever as off-site authority (a BYR job, next thread).

**Date wrapped:** 2026-06-19
**Theme version at wrap:** 1.0.620 (pushed) · **HEAD = `b8592f0`**
**WordPress:** 7.0 · **Astra:** 4.13.x · **Plugins:** 5 (AIOSEO, Converter for Media, Hostinger Tools, LiteSpeed Cache, MCP Adapter)
**Live:** https://thomascheesman.ca

Successor to [V0.33.md](V0.33.md) (comments, button standard, fonts, RISK-1) and [V0.31.md](V0.31.md) (the authoritative audit + OD-1 privacy reference). Next agent writes **V0.35**.

---

## ⚠️ Read this first

- **⭐ ONE owner action is pending and gates the *visual* of two changes this session:** Thomas needs to **wp-admin → LiteSpeed Cache → Toolbox → Purge → Purge CSS/JS Cache** (the API purge can't clear the combined bundle). Until then the **kinetic-hero a11y fix (1.0.619, main.js)** and the **HCS FAQ accordion styling (1.0.616, CSS)** are live in the markup but won't *render/announce* correctly. Everything else (HTML/schema/PHP) is already live via the API page-cache purge.
- All theme code committed AND pushed (TC auto-push). This thread ran **1.0.603→… wait, 1.0.615 → 1.0.620** across 6 version-bumping commits + 1 docs commit (this file, no bump).
- **The audit's Claude-buildable items are now essentially exhausted.** What remains is owner decisions (OD-series), owner hands-on (backlinks, off-host archive copy, keepsake uploads, Family accounts, real-device pass), and the **BYR audit** (its own session in the `bareyr` repo — do NOT do BYR work from this repo; cross-site rule).

---

## This session's work (1.0.615 → 1.0.620)

| Ver | Commit | What |
|---|---|---|
| 1.0.616 | `36ac9b4` | **HCS FAQ** — visible `<details>` accordion + `FAQPage` JSON-LD, one source of truth in `inc/hcs-faq.php`; rendered on `/hcs` between the BYR bridge and Resources; `/hcs/#faq` deep-links. Every answer distilled from the essay's own prose (no new medical claims). |
| 1.0.617 | `4b877da` | **Author E-E-A-T + retro-links** — the `author` Person node on the 8 long-reads + `/hcs` Article schema now carries `description` + `knowsAbout` inline (so a single cited URL self-asserts expertise for RAG/AI retrieval); `/about` → `/family/thomas` ("the long way round"); `/hcs` "heritage section" → `/family/heritage`. `inc/seo-overrides.php`, `page-about.php`, `page-hcs.php`. |
| 1.0.618 | `e83075c` | **`/hcs` → `/family/thomas`** retro-link in the "What it gave" section, in Thomas's own wording. Completes the V0.20 retro-link set (Cheesman long-read still pending — needs the off-repo regen pipeline). |
| 1.0.619 | `ad45cb2` | **A11Y-3 fix** — `splitIntoCharSpans()` (main.js) now self-heals the kinetic hero's `aria-label` from its text before splitting into aria-hidden char spans. Fixes the lone axe finding (`empty-heading` on inner-page `<h1>`s). Home unaffected (keeps its PHP label). **Needs the CSS/JS purge to go live.** |
| 1.0.620 | `b8592f0` | **A11Y-3 follow-up** — `aria-label` on the desk-menu search input + the arcade name-entry input (were "edit, blank"). Pure a11y; desk menu is BHAG but label-only, greenlit. |
| — | (this) | **V0.34** handoff (docs; no bump). |

### HCS FAQ ([inc/hcs-faq.php](inc/hcs-faq.php))
`tc_hcs_faq_items()` is the single array → both `tc_render_hcs_faq()` (accordion, `.about-section` chrome, `.hcs-faq` CSS in style.css) and `tc_render_hcs_faq_schema()` (wp_head, gated `is_page('hcs')`, mirrors the `tc_render_person_schema` pattern; answer text = visible answer with tags stripped, so schema == page). 9 Q&As. To edit: change the array; both surfaces follow.

### The AI-SEO reassessment (no code; the strategic finding)
Thomas brought a Grok thread on Google AI-SEO. Grounded read after reading the actual code: **TC's on-page AI-SEO is already near-maxed** — Grok's "add Article/Person/FAQ schema" recommendations were *already built* (Grok audited from outside and couldn't see the JSON-LD). The site already has: Article schema (author + dates) on the 8 long-reads + `/hcs` (with a YMYL-safe `about: MedicalCondition`, typed Article-not-MedicalWebPage), homepage Person, FAQPage (now), AIOSEO meta/OG/breadcrumb, llms.txt, TRUST-1 colophon. **The real lever is off-site authority (backlinks)** — which compounds hardest on **bareyourrare.org**, not the personal site (most of TC is intentionally noindexed/gated per OD-1). Conclusion: the highest-value next move is a **BYR audit in its own session**; TC needed only the small schema/retro-link polish done here.

---

## 📊 PERF-3 — baseline (done; measurement, no code change)
Captured via **local Lighthouse** (`npx -y lighthouse … --chrome-flags="--headless=new"`, CHROME_PATH = the installed Chrome). The keyless **PSI API 429'd** from this sandbox IP — use local Lighthouse, not the API, unless Thomas supplies a PSI key. **No CrUX *field* data exists** (site below Google's real-user traffic threshold) → lab baseline only (itself a finding).

| Page | Device | Perf | LCP | CLS | TBT (≈INP) | FCP |
|---|---|---|---|---|---|---|
| `/` | mobile | **79** | 3.3 s | 0.013 | 150 ms | 3.0 s |
| `/` | desktop | 98 | 0.4 s | 0.001 | 10 ms | 0.4 s |
| `/hcs/` | mobile | **82** | 3.4 s | 0.013 | 190 ms | 2.1 s |
| `/hcs/` | desktop | 97 | 0.7 s | 0.001 | 0 ms | 0.6 s |

**Read:** desktop near-perfect; **CLS + TBT/INP green everywhere**; the only weak spot is **mobile LCP/FCP**, driven ~80% by **server response time / TTFB** (root doc ~1,290 ms home / ~790 ms hcs — likely a cold/uncached test; an owner LiteSpeed cache-warmth issue, not theme code) + render-blocking CSS (owner: LiteSpeed "Generate Critical CSS" + "Load CSS Async" — a runtime fix that respects the no-bundling rule). Secondary: `/hcs` images served ~244 KB larger than displayed (a real theme-side win — right-size/`srcset` the heaviest figures). **Don't** chase "unused CSS" via bundling (intentional modular choice).

## ♿ A11Y-3 — automated pass (done) + manual status
- **axe-core** (via `puppeteer-core` + the installed Chrome) across `/`, `/hcs/`, `/family/`, heritage hub, a long-read: **home 0 violations; the only finding anywhere was one MINOR `empty-heading`** on inner-page `<h1>`s → fixed (1.0.619). No critical/serious/moderate anywhere.
- **Accessibility-tree dump** (Chrome CDP via puppeteer) confirms: full content exposed on the base page; the **desk menu opens as a proper `role=dialog` / `aria-modal` named "Thomas's desk — site navigation" with 117 labelled focusable items** (hotspots announce "Grumpy toad — open the arcade", etc.). The decorative canvases are correctly `aria-hidden`. The only gaps were the two unlabelled inputs → fixed (1.0.620).
- **Manual NVDA pass:** attempted with Thomas; inconclusive due to screen-reader *driving* difficulty on his end (focus staying in the browser chrome; "blank" on the title = the un-deployed 1.0.619 heading fix). **A11Y-3 is declared effectively complete on the strength of the automated tree + axe proof.** A real NVDA/VoiceOver walk of the desk menu + map remains a nice-to-have if a confident SR user is ever available.

---

## ✅ Audit ledger — current status
**Done:** SEC-1 · SEO-2 · SEO-3 · AEO-1 · IA-1 · A11Y-1 · A11Y-2 (focus trap) · **A11Y-3 (this session)** · PERF-1 · PERF-2 · **PERF-3 (this session)** · CODE-1 · TRUST-1 · RISK-1 (built) · OD-1 · OD-5 · **HCS FAQ (this session)**.
**Still open — owner decisions:** SEC-2, OD-2, OD-3, OD-4, OD-6 (OD-6 partly met by RISK-1).
**Still open — owner hands-on:** copy `site-archive/` off-host · re-submit Cheesmans to Wayback · upload the 9 public keepsake PDFs · create the ~10 Family accounts · **MOB-1** real-device pass · **backlinks** (the #1 finding → BYR session).
**Still open — small Claude items:** A11Y-2 map *visible* keyboard text-equivalent (enhancement, BHAG); `/hcs` image right-sizing (PERF-3 surfaced ~244 KB); Cheesman long-read retro-link (needs the heritage regen pipeline). Quick verifies: default `og:image` (set), trash duplicate `map-2` page (`id 3920`).
**Deferred late-stage (his call):** protected-uploads layer (gated kid media still fetchable at direct URLs); the two YouTube docs feature the kids (left public).

Non-audit backlog (unchanged): **Melanie's page** (Thomas said skip for now), collection Intro / his Conclusion, research chases (Steinke/Haiste/ScotlandsPeople/Dan interview), map data fills + desk-menu object, GEDCOM/Dochertys print book, easter-egg tail. Full list: [[project_tc_site_backlog]].

---

## 🩹 Sharp edges (new this session + carried)
- **⭐ NEW — PSI API is keyless-429 from this environment.** For perf baselines use **local Lighthouse via npx** (`CHROME_PATH="C:/Program Files/Google/Chrome/Application/chrome.exe" npx -y lighthouse <url> --only-categories=performance --output=json --output-path=… --chrome-flags="--headless=new --no-sandbox --disable-gpu"`; `--preset=desktop` for desktop). Node 22 + Chrome are installed.
- **⭐ NEW — automated a11y is easy here:** `puppeteer-core` + `axe-core` + the installed Chrome; `page.accessibility.snapshot()` gives the screen-reader tree headlessly. Scratch scripts were in `%TEMP%/axe/` (gitignored-by-location; not in repo).
- **⭐ NEW — desk menu is AJAX-injected (G7),** so its markup edits are PHP served fresh — they go live on the normal API page-cache purge (no CSS/JS-bundle purge needed). Only CSS/JS source changes (main.js, style.css) need Thomas's Toolbox purge.
- **Combined CSS/JS cache** — API purge doesn't clear it; CSS/JS changes need the Toolbox purge or TTL. (The #1 deploy-confusion source.)
- Carried: live theme dir = `tc-ventures-child-theme`; privacy load-bearing (OD-1); "parents" never "co-parents"; bump `style.css` Version with the **Edit tool** on theme-code commits (docs don't bump); explicit `git add` paths (tree carries `.docx`/`.xlsx`/`.mp4`/`build-*.py`/`site-archive/`); never `object-fit/background-size:cover`; CSP is `.htaccess`-only (keep `'unsafe-eval'` for wp-admin); secrets entered by Thomas only; auto-push is **TC-only**; **BHAG surfaces (desk menu, drawer, map) = plan/propose**.

---

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`). **Read `CLAUDE.md`, then `V0.34.md` (latest), then `V0.33.md` and `V0.31.md`** (V0.31 = authoritative audit + OD-1 privacy reference). V0.34 shipped the HCS FAQ (`FAQPage`), author E-E-A-T schema, the `/family/thomas` retro-links, and the PERF-3 + A11Y-3 passes — **the audit's Claude-buildable items are now essentially done.**
>
> **Purge cache yourself** via `tc-portfolio/purge-cache` after each push — but it does NOT clear the combined CSS/JS bundle, so CSS/JS-source changes need Thomas to "Purge CSS/JS Cache" in the LiteSpeed Toolbox. **⚠️ One such purge is currently pending** (the 1.0.619 heading fix + 1.0.616 FAQ styling). Perf baselines: use **local Lighthouse via npx** (PSI API 429s keyless here). Automated a11y: `puppeteer-core` + `axe-core` + the installed Chrome.
>
> **What's left:** mostly owner-side (backlinks, off-host archive copy, keepsake uploads, Family accounts, MOB-1) + owner decisions (OD-2/3/4/6, SEC-2). The **BYR audit is the highest-value next move — but do it in its own session in the `bareyr` repo** (never cross-pollute from here). Small TC items if asked: A11Y-2 map keyboard text-equivalent, `/hcs` image right-sizing, the Cheesman long-read retro-link (heritage regen pipeline). Melanie's page is parked at Thomas's request.
>
> **Hard rules:** plan/propose on BHAG surfaces (desk menu, drawer, map); kid-privacy (OD-1); "parents" never "co-parents"; bump `style.css` Version (Edit tool) on theme-code commits; explicit `git add`; never `object-fit/background-size:cover`; `.tc-btn` is the button standard; secrets entered by Thomas only; auto-push is TC-only.
