# V0.39 — the **ARCADE-COMPLETE PUSH** session (Back Quarter 3D, 1.0.684 → 1.0.706)

Twenty-three theme versions in one arc. The 3D Back Quarter went from
"scaffolding that boots" to a living farm: real MX race track with lap
ghosts, real recorded audio with positional sound, tone-mapped sky with
shadows and a day/night cycle, a windmill / flags / pumpjack heritage
layer, a full barnyard, and a filled-in quarter (creek, bog, firepit,
camper, stock barn, hedgerows).

**Date wrapped:** 2026-07-09
**Theme version at wrap:** 1.0.706 (pushed) · **HEAD = `68d9cda`**
**WordPress:** 7.0 · **Astra:** 4.13.x · **Live:** https://thomascheesman.ca

Successor to [V0.38.md](V0.38.md). Next agent writes **V0.40**.

---

## ⚠️ Read first — where Thomas wants to go next

1. **RIDE THE HORSE.** Thomas wants to swap buggy → horse. Agreed
   approach (proposed, not built): it's a **vehicle swap**, not new
   physics — Enter near a horse parks the buggy, camera/controls attach
   to the horse with horse numbers (slower cap, tighter turn, gallop
   bob, hoofbeats instead of engine, Space = leap; maybe horses can hop
   the compound fence as a family shortcut). ~a session of work.
2. **GRAPHICS PHASE D — BLOOM + EMISSIVE.** The agreed plan's biggest
   remaining visual: vendor Three r128 EffectComposer + UnrealBloomPass,
   convert the "lights" (windows/lanterns/bulbs/screens/flare/fire) to
   emissive so they actually glow. Phases A–C (audio, tone mapping +
   sky, shadows) are DONE. Phase F (quality tiers) folds into 3D-P3.
3. **3D-P3 target devices:** PCs + capable tablets ONLY, **never
   phones** (Thomas, 2026-07-07 — in auto-memory). Phones keep the 2D
   board + ledger.
4. Feel-test asks may come back on: putt-putt idle character, brush
   density (knobs: liner spacing `95`, cluster count `16`), berm
   height, day-cycle pace (`DAY_CYCLE` 480s).

---

## What shipped this session (by theme)

### The Section Road (race track) — 1.0.688–690, 696, 702
- Dirt ring **outside** the fence; world walls moved out (OUT 430);
  the perimeter fence is now solid physics and the farm **gate at
  bottom centre is the only way out**. Start/finish (checkered strip)
  on the south straight, dead ahead of the gate.
- **Timed laps**: cross the line (either direction), hit 3 corner
  checkpoints in order, come home. Live time + best in HUD; corner
  ticks; NEW-BEST fanfare.
- **Ghosts**: gold = best ever, silver ×2 = recent, 20 Hz [x,z,angle]
  streams interpolated on replay. localStorage **`tcBqLaps_v2`**.
- **Motocross build**: track width 128; corners BULGE out from the
  fence with **banked berms** (`bermAt` — low lip on straights, tall
  wall in corners, baked into ground mesh AND heightAt); rhythm
  tabletops + a kicker (JUMPS system), whoop washboard on the north
  straight, 2 dimple swales, speed-up pads on all four straights.
- **Stadium light towers** outside the corners (330-high, PointLights).
- **Grandstands** ×2 on the north straight: 200-person instanced crowd
  that bobs/cheers; heritage **drive-in billboards** moved outside the
  track on tall legs (panel ~246 up) with marquee **glass-bulb chase**
  (one InstancedMesh, colors re-chased every 300ms) and **old-country
  FLAGS** on poles (mixed lines fly two: Docherty IE+SCT, Steinke
  DE+PL).

### Audio — 1.0.691, 701, 703 (+705 moo fix)
- **Synth pass**: wind bed w/ gust LFO, surface-aware tire roll,
  splash/mud/thud, crickets, crowd, race beeps, fanfare.
- **Real samples** (Thomas's "1980s driving game" review → option 3a):
  10 recordings in `assets/audio/bq-*.mp3` (~340 KB), lazy-fetched on
  first sound-on, **every caller falls back to synth** if a buffer is
  missing. Sources = Wikimedia Commons PD/CC0 **except** duck (Jonathon
  Jongsma, CC BY-SA 3.0) and moo (MichaeltheFox8621, CC BY-SA 4.0) —
  **credits live in `assets/audio/CREDITS.md` + a comment atop the
  audio section; keep them in lockstep if files change.**
- **Positional audio**: PannerNodes (world/10), listener rides the
  buggy; hens FROM the coop, applause FROM the stands, windmill creak
  FROM the shore, pumpjack clank per beam stroke. **Rooster crows at
  sunrise.** Cows moo / horses whinny as you pass; Trumac = pitched-
  down moo.
- **Engine**: diesel loop under a ducked synth bass, rate-bent by revs,
  both through the **putt bus** — square-wave LFO chops gain at firing
  rate (8.5 Hz idle putt-putt → ~30 Hz flutter at speed). Audible idle.

### Graphics + feel — 1.0.692–695, 698–700
- **ACES tone mapping**, gradient **sky dome + starfield + haloed
  moon** (rides the camera), fog to horizon color.
- **Shadows**: PCFSoft 2048 moonlight whose frustum **follows the
  buggy** (fixed light direction); Lambert meshes cast/receive; the
  old circular blob shadow is REMOVED.
- **Day/night**: 8-min cycle (`DAY_CYCLE`, starts at dusk via
  `DAY_START`); one df factor drives sky uniforms, fog, lights, stars,
  sun/moon positions. Night dimmed from the original.
- **Drive feel**: eased/loosened steering (steerVal smoothing, rate
  .06, grip .84); body dynamics → then **whole-buggy terrain
  alignment** (wheels ride pitch/camber; body adds lean/squat/dip;
  signs documented in-code — camber is −atan(latSlope)). Gravity
  ended at **476/437 boosted** (three raises). Launch = climb-based,
  cap 185. **Big air pays turbo** on clean landings >0.85s. Chase cam
  LOW (54 up, looking 14 high).

### The world — 1.0.697, 699, 700, 702, 704–706
- **Heritage**: Dutch **WINDMILL** 2.5× on the slough shore (sails
  turn, → /lakemans) + tulip rows; **pumpjack** 2.5× + flare stack
  (flicker + orange light); heritage flags (above).
- **Barnyard**: coop 2× in the NE corner (a token INSIDE the pen),
  12 chickens +5% incl. a **black rooster**, pigs + mud pit, 7 cows +
  **Trumac** (black bull 1.3×, roaming prompt), 2 horses, 5 ducks
  floating the slough, tractor drives itself (slow, dusty).
- **Fill-in (1.0.704)**: **CREEK** out of the slough (painted like
  roads, fords two of them, shallow-wade physics), **THE BOG**
  (340×140 rutted wallow + mud humps — mudbogging), mud pit face-lift
  (shared `buildMudPatch`), **firepit hangout** (live fire + light,
  log benches, firewood stack, **camper**), **open STOCK BARN**
  (U-shaped physics, animals wander in), church grounded (was floating
  on its deleted mound's ghost).
- **Brush (1.0.705–706)**: 256 plants, 3 varieties (bushes / berry
  bushes / **apple trees with solid trunks**), placed as **hedgerows
  lining every road + 16 bluffs seeded into voids**. Six instanced
  draws. Fresh layout per boot.
- **Tokens**: 30 total (`tcBqTok_v1`, **append-only — saved indices
  must hold**). All-found = **fireworks in the kids' colors + applause
  + fanfare**; the **restack pad resets the hunt** when complete.

### The ledger + site (1.0.687)
- Spec §5 shipped: `inc/data/quarter-section.json` feeds (1) the
  homepage **ledger strip** (replaced the pillars; pillar-card CSS
  stays for subpages), (2) the **mailbox flag on BOTH boards** (up
  while newest entry < 21 days; Enter at the mailbox scrolls to the
  strip), (3) the drive-ins' **NOW SHOWING verbatim pull-quotes**.
  `tc_bq_ledger_data()` in functions.php, localized front-page-only.
  **Update the JSON with every content push** — same discipline as the
  version bump.

---

## 🧭 3D state reference (read before touching)
- **One file:** `assets/js/back-quarter-3d.js` (~3,900 lines). World
  data at top: `LANDMARKS` (per-lm `scale` grows mesh + Matter body +
  prompt range together), `LINES` (+flags), `PATHS`, `TRACK`,
  `LAP_CKPTS`, `HILLS`, `POND`/`pondR`, `COOP`, `PIGPEN`, `MUD`,
  `BOG`, `CREEK`, `FLAT` (**add one per new building**), `MOUNDS`,
  `RAMPS`, `JUMPS` (via `generateTrackJumps`), `PADS`, `TOKENS`
  (**append-only**).
- **Terrain stack:** `rawHills` → `hillsAt` (FLAT) → `heightAt`
  (+mounds +rampAt +bermAt +jumpAt). `bermAt`/`jumpAt` early-out via
  `nearTrack()` (outside-the-fence only). Ground mesh bakes
  hillsAt+bermAt + painted roads/track/creek at boot.
- **Jumps:** `jumpProfile()` drives physics AND mesh — edit once.
- **Audio:** everything behind `audio.on`; samples lazy via
  `loadSamples()`; synth fallbacks everywhere; sound OFF by default.
- **Buggy pose:** group carries terrain pitch/camber, chassisGroup
  carries lean/squat/dip; flips own the pose while airborne.
- ⭐ Verification limit: automation tabs freeze rAF/WebGL — and can
  even stall vendor fetches (seen 2026-07-08; a FRESH tab fixed it).
  Smoke-test = boots clean, zero console errors; Thomas drives.

## Sharp edges (new + carried)
- **⭐ TOKENS and lap-ghost keys:** append tokens only; if track
  geometry changes fairness, bump `tcBqLaps_v2` → v3.
- **⭐ BY-SA credits** (duck, moo) must survive any sample swap —
  CREDITS.md + JS comment in lockstep.
- **⭐ quarter-section.json** is part of the content-push checklist.
- **The "invisible button text" bug is DOCUMENTED** in style.css above
  `.bq-fs`: every custom button gets `color` + `-webkit-text-fill-color`,
  repeated under `:fullscreen`. Confirmed fixed on the stage buttons.
- Chrome extension console noise ("message channel closed") is NOT a
  site error — filter it out mentally.
- Deploy-poll greps: match a string that's actually in the FILE (a
  case mismatch cost 10 minutes this session).
- Carried: `/#bq3d` is the only door until 3D-P3; `?cb=Date.now()` on
  the 3D file → API purge suffices; explicit `git add` paths; Edit-tool
  version bumps; `object-fit/cover` never; "parents" never
  "co-parents"; BHAG = plan-first; CSP is .htaccess-only; purge after
  every push.

## Backlog (rough priority)
1. **Horse riding** (vehicle-swap plan above — Thomas wants it).
2. **Graphics Phase D**: bloom + emissive (vendor postprocessing).
3. **Phase E polish**: pond material (still flat blue), palette grade,
   volumetric headlight cones, ground detail.
4. **Phase F / 3D-P3**: quality tiers + tablet/PC gate (NO phones),
   then 3D becomes the public homepage, 2D demotes to fallback.
5. Animals shelter in the stock barn at night (day-cycle behavior).
6. Feel-tuning follow-ups as Thomas reports (putt, brush density,
   berms, day pace).
7. Carried from V0.38: TC's Rants / old wordpress.com posts (drafts
   invisible to MCP — read via logged-in wp-admin or Thomas exports);
   December reading photo → 7th hero ticket (~November); confirm kid
   signature colors.

## Opening prompt for the next agent

> I'm picking up the TC personal site theme (`tc-ventures-child-theme`).
> **Read `CLAUDE.md`, then `V0.39.md` (latest), then
> `docs/QUARTER-SECTION-SPEC.md`.** The homepage BHAG is deep in
> Phase 3D: a public 2D board plus a full 3D farm + race track behind
> **`thomascheesman.ca/#bq3d`** (engine: `assets/js/back-quarter-3d.js`,
> data-driven const blocks up top; physics Matter 2D, render vendored
> Three r128; loads fresh via `?cb=Date.now()` so the API
> `tc-portfolio/purge-cache` is enough). **Start with V0.39's Read
> First:** Thomas wants HORSE RIDING (vehicle-swap plan is written) and
> the BLOOM pass next; 3D targets PCs + tablets only, never phones.
> Verify 3D changes by confirming a clean boot in a FRESH automation
> tab (they freeze WebGL and sometimes stall fetches) — Thomas does the
> feel test.
